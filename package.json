{
  "name": "ocp-getting-started-content",
  "version": "0.0.0",
  "private": true,
  "description": "Antora/Showroom content build for the OpenShift Application Platform \u2014 Getting Started workshop (workshop / demo / instructor renderings).",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:workshop": "cd content && antora --fetch site-workshop.yml",
    "build:demo": "cd content && antora --fetch site-demo.yml",
    "build:instructor": "cd content && antora --fetch site-instructor.yml",
    "build": "npm run build:workshop && npm run build:demo && npm run build:instructor",
    "serve": "utilities/lab-serve"
  },
  "devDependencies": {
    "@andrew-jones/antora-tabs-extension": "1.0.0",
    "@antora/cli": "3.1.15",
    "@antora/site-generator": "3.1.15",
    "@sntke/antora-mermaid-extension": "0.0.13"
  }
}
