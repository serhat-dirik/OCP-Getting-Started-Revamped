{
  "//": "M03 (Dev Spaces & the Inner Loop): che-code prompts to install these on first open so the debug exercise works. redhat.java = Language Support for Java; vscjava.vscode-java-debug = Debugger for Java. If this cluster's extension registry (Open VSX) is offline, the install is unavailable and the GUI debug step falls back to the jdb terminal path in the lab.",
  "recommendations": [
    "redhat.java",
    "vscjava.vscode-java-debug"
  ]
}
