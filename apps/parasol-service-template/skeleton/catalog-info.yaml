# Backstage / Red Hat Developer Hub catalog entity for the scaffolded service.
# Registered automatically by the template's catalog:register step.
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Parasol service scaffolded from the golden-path template.
  annotations:
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-id: ${{ values.name }}
    argocd/app-name: ${{ values.name }}
    janus-idp.io/tekton: ${{ values.name }}
  tags:
    - java
    - quarkus
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}
  system: parasol-insurance
