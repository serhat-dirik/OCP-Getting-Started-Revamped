# Test-only dependencies for test_app.py - NOT part of the runtime image (the
# Containerfile only ships the node/ implementation; python/ is built by its own S2I
# import from requirements.txt, which this file is deliberately separate from so CI
# test tooling never ends up on the running service's classpath).
#
# Pinned for reproducible CI runs (verified on PyPI 2026-07-30).
# httpx backs FastAPI's TestClient (starlette dependency, not installed by
# requirements.txt alone).
pytest==9.1.1
httpx==0.28.1
