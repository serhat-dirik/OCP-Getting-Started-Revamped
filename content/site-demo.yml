---
# Antora playbook — DEMO rendering (SA presenter guide).
# Identical to site-workshop.yml except: site.title suffix, `demo: true` attribute
# (unlocks ifdef::demo[] Say/Show/Do presenter blocks), and output.dir.
# Build from inside content/ (see site-workshop.yml for why): `npm run build:demo`.
site:
  title: "OpenShift Application Platform — Getting Started — Demo Guide"
  start_page: modules::index.adoc

content:
  sources:
    - url: ..
      start_path: content

asciidoc:
  attributes:
    demo: true            # renders ifdef::demo[] presenter blocks throughout the guide

ui:
  bundle:
    url: https://github.com/rhpds/rhdp_showroom_theme/releases/download/patternfly-6/ui-bundle.zip
    snapshot: true

antora:
  extensions:
    - require: '@sntke/antora-mermaid-extension'
      mermaid_library_url: https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.esm.min.mjs
      script_stem: header-scripts
      mermaid_initialize_options:
        start_on_load: true
        # Legibility defaults (Serhat 2026-07-10: diagrams rendered too small). Larger base
        # font + generous node/rank spacing so text stays readable; useMaxWidth keeps diagrams
        # responsive to the content column. Wide graph-LR diagrams still want a per-diagram
        # look during the media pass — this is the global floor, not the ceiling.
        theme: default
        themeVariables:
          fontSize: "18px"
        flowchart:
          htmlLabels: true
          useMaxWidth: true
          nodeSpacing: 45
          rankSpacing: 50
          padding: 12
    - require: '@andrew-jones/antora-tabs-extension'

runtime:
  cache_dir: ./.cache/antora

output:
  dir: ../www/demo
