// Prep reminder for the top of every module's INSTRUCTOR page.
//
// WHY THIS EXISTS (owner direction, 2026-08-06): companion to partial$prep-while-you-read.adoc,
// which prompts the ATTENDEE from the concept page. This one prompts the PRESENTER from the
// instructor page — an instructor working through Pre-flight/Timing/Watchouts on a cluster that
// was never prepped needs the same nudge before anything below assumes a running environment.
//
// Usage — :module-id: is already set on every instructor page (same as concept pages), so this
// is a single include near the TOP of the page, before the first section heading:
//   include::partial$prep-before-you-teach.adoc[]
ifndef::module-id[:module-id: unset-module-id]

[TIP]
.Before you teach this module
====
If you haven't already prepped it, run this first to bring up the lab environment:

[source,sh,role=execute,subs="attributes"]
----
ws prep {module-id}
----

It is safe to run at any time: it checks what is already in the project, tells you if anything is
left over from an earlier module, and *asks before wiping* anything.
====
