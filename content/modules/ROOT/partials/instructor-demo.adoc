// The [INSTRUCTOR-DEMO] callout — marks a segment the instructor performs live
// (per 04-STYLE-GUIDE §3). Renders distinctly in ALL flavors (workshop, demo,
// instructor) via the `instructor-demo` role, which the supplemental UI styles.
// Usage — set the body (and optional title) on the page BEFORE including:
//   :demo-title: The cluster blocks an unsigned image
//   :demo-body: The instructor pushes an unsigned build; admission refuses it on screen.
//   include::partial$instructor-demo.adoc[]
// For multi-paragraph segments, author the styled block directly instead — see
// docs/authoring-conventions.md (section "Instructor-performed demos").
ifndef::demo-title[:demo-title: Instructor-performed demo]
ifndef::demo-body[:demo-body: (The instructor performs this segment live; attendees watch.)]

[NOTE,role=instructor-demo]
.🧑‍🏫 Instructor demo — {demo-title}
====
{demo-body}
====

// Reset so a later include on the same page must set its own body.
:!demo-title:
:!demo-body:
