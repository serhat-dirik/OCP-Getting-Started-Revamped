%% Step 2 of the demo: one write, and what does and does not follow it. Five ranks LR, so the
%% 900px column renders it at about 1x rather than upscaling a tall graph.
flowchart LR
  put["bao kv put<br/>a new password"]
  ver["Vault metadata<br/>version 1 becomes 2"]
  eso["External Secrets Operator<br/>re-checks every 15s"]
  sec["Secret/claims-creds<br/>new value within seconds"]
  proc["Running process<br/>keeps the old env var"]

  put --> ver --> eso --> sec
  sec -.->|"env is read at start"| proc
