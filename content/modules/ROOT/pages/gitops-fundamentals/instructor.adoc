= GitOps Fundamentals: Instructor Guide
:module-id: gitops-fundamentals
include::partial$version-attributes.adoc[]

// Internal delivery runbook. Timings MEASURED on the build cluster (OCP 4.21.22 / OpenShift
// GitOps 1.21.1, Argo CD 3.4) as user7 on 2026-07-10/11 by performing every exercise and
// timing the operations. The student Argo CD instance is SHARED; read "Watchouts → controller memory"
// AND "Watchouts → self-heal backoff" before running this module for a room, and especially before
// rehearsing the demo flavor — a repeat drift against the same Application/revision is slow by design.

The module's signature beat is exercise 3: a console `oc scale` that Argo CD *erases* — first when you press Sync, then, with self-heal on, on its own in a few seconds.
Both reverts are fast (self-heal measured ~4s, watch-triggered), so the drift-revert arc lands *live* with no waiting to narrate around.
One platform note: the student Argo CD instance is shared, and its application-controller must have adequate memory (2&nbsp;Gi on this cluster) — see Watchouts.

include::partial$prep-before-you-teach.adoc[]

== Timing

Total hands-on: *~40 min* (add ~12–15 min of concept up front).

[cols="1,4,1,2",options="header"]
|===
| # | Exercise group | Attendee time | Measured core (as user7, 4.21.22)

| 1 | Git is the source of truth | ~5 min | read-only (`git clone`, `cat`, `oc kustomize`) — seconds
| 2 | Create your first Application | ~11 min | UI form + first Sync: 7 resources applied in *3s*, fully *Healthy in 26s* (warm image cache); `oc apply` denial instant; the NEW APP card can take ~90s to first populate (cold repo clone)
| 3 | Drift and self-heal | ~15 min | drift → OutOfSync immediate; *manual Sync reverts in seconds*; *self-heal reverts a fresh drift in ~4s* (watch-triggered; `appSync: 90s` is the ceiling)
| 4 | Git-driven change (stage) | ~9 min | stage first sync ~30–60s; after a git edit, Refresh re-reads `main` in *~20s* → OutOfSync, Sync converges in seconds
|===

[NOTE]
.How this timing was measured
====
Every exercise was performed as `user7` on the build cluster (OCP 4.21.22, OpenShift GitOps 1.21.1, Argo CD 3.4) on 2026-07-10/11, driving the Argo CD control plane the way the UI does and timing each operation.

* *First sync (exercise 2)* — the sync *operation* completed in *3 seconds* (all seven objects created); full rollout to *Healthy* took *26 seconds* on a warm cluster (the `parasol-claims:1.0` image was already cached).
On a *cold* node add the image pull (a few minutes) for the first user of the day.
The NEW APP card can take up to ~90s to first populate while the repo-server clones the fork for the first time.
* *Manual Sync revert (exercise 3)* — the `replicas` field reverts to the Git value within seconds of clicking Sync; the operation shows a longer *Duration* only because it waits for the extra pods to terminate and the rollout to settle.
* *Self-heal revert (exercise 3)* — measured *~4 seconds* from `oc scale` to reverted, three trials (4s / 4s / 4s), on the 2&nbsp;Gi controller with `appSync: 90s`.
Self-heal is *watch-triggered* — Argo CD reacts to the live change, not a poll — so the 90s reconcile interval is only the ceiling, never the typical revert time.
That ~4s figure is for a *fresh* drift on a revision Argo CD has not just synced.
A *repeat* drift against the same Application and the same Git revision runs into Argo CD's own self-heal backoff and can take minutes — see Watchouts and Troubleshooting.
(An earlier build measured ~190s; at the time that was attributed to the controller OOMing at 1&nbsp;Gi, since fixed — see Provenance for why that attribution is no longer certain.)
* *Git-driven change (exercise 4)* — after a commit to `main`, a UI *Refresh* re-resolved the branch to the new commit in *~20s* and the app went OutOfSync; Sync then converged in seconds.
====

== Pre-flight checklist

Run these *before the session* (as cluster admin unless noted).

. *A sample user's entry state materializes and verifies.*
+
[source,sh,role=execute]
----
ws start gitops-fundamentals --user user1
ws verify gitops-fundamentals --user user1 --entry-only
----
+
Expect `✅ all 8 checks passed`: the `user1-gitops` namespace + entry marker, the student Argo CD instance reachable, the `proj-user1` AppProject, the Gitea fork `user1/claims-config`, its `overlays/dev` personalized to `user1-dev`, and `user1-dev`/`user1-stage` *empty* (the attendee's Application is the deploy).

. *The student Argo CD instance is healthy — check the controller is NOT crash-looping.*
+
[source,sh,role=execute]
----
oc get pods -n ogsr-student-gitops
oc get pod student-gitops-application-controller-0 -n ogsr-student-gitops \
  -o jsonpath='ready={.status.containerStatuses[0].ready} restarts={.status.containerStatuses[0].restartCount}{"\n"}'
----
+
Expect all five pods `Running` and the application-controller `ready=true` with a *low, stable* restart count.
A climbing restart count or `CrashLoopBackOff` means the controller is OOMKilling (see Watchouts) — sync and self-heal will be unreliable until its memory is raised.
*Do not run the module for a room in that state.*

. *Per-user isolation is enforced (the tenancy the lab relies on).*
+
[source,sh,role=execute]
----
argocd admin settings rbac can user1 sync applications 'proj-user1/x' --namespace ogsr-student-gitops
argocd admin settings rbac can user1 sync applications 'proj-user2/x' --namespace ogsr-student-gitops
----
+
Expect `Yes` then `No` — user1 may sync their own project's apps and *not* another user's.
This is what makes a shared Argo CD instance safe for a room.

. *The fork exists and its overlays are personalized.*
+
[source,sh,role=execute]
----
GITEA=$(oc get route gitea -n ogsr-gitea -o jsonpath='{.spec.host}')
curl -ks "https://$GITEA/api/v1/repos/user1/claims-config/raw/overlays/dev/kustomization.yaml?ref=main" | grep namespace:
----
+
Expect `namespace: user1-dev`.
If it still reads `__user__`, the fork job did not personalize — `ws reset gitops-fundamentals --user user1`.

. *(For a demo) pre-solve a sample user — it blocks ~4–6 min by design.*
+
[source,sh,role=execute]
----
ws solve gitops-fundamentals --user user1     # creates the dev+stage Applications; BLOCKS until both Healthy
----
+
[IMPORTANT]
.`ws solve gitops-fundamentals` blocks for ~4–6 minutes — that is correct, not a hang
=====
`ws solve` creates the two student Applications (`claims-dev-user1`, `claims-stage-user1`) and *waits* for both to be Synced/Healthy before returning — it does not print "demo-ready" until the claims app is actually up in both `user1-dev` and `user1-stage` (image pull + `claims-db` init + app startup, two environments).
The solve apps ship `selfHeal: true` and a cascade finalizer, matching the end state GitOps at Scale builds on.
Do not Ctrl-C it thinking it stalled.
=====

== Demo-mode notes

Beyond the Say/Show/Do arc in the demo flavor:

* *Pre-solve and pre-sign-in before the room.*
Run `ws solve gitops-fundamentals --user {user}` early (blocks ~4–6 min) so a finished, Healthy `claims-dev-{user}` is your opening visual, and sign in to the student Argo CD UI as `{user}` with *Log in via OpenShift* ahead of time — the OpenShift login redirect eats 20–30 seconds you do not want live.
* *Both reverts are fast — the whole drift arc lands live.*
Beat 3 (drift → click Sync → snaps back to 1) is instant.
Beat 4 (enable self-heal, drift, it reverts unaided) lands in a few seconds — watch-triggered, measured ~4s — so there is nothing to wait out; let it happen on screen and let the room see the replica count drop by itself.
* *You are already two sync cycles deep before you say a word — a rehearsal or the Challenge can tip you into backoff.*
`ws solve` performs one sync to the entry Git revision as part of its own prep, and if you privately rehearse beats 3–4 before the room, that rehearsal is a second sync/self-heal cycle against that *same* revision.
Argo CD's own self-heal backoff (see Watchouts, Troubleshooting) applies per Application per revision, so the live beat 4 drift can land slower than the ~4s you rehearsed with, and the optional Challenge's config-map-deletion drift (same app, same still-unchanged revision) is a further repeat on top of that.
If you must rehearse, either commit a trivial real Git change afterward (a new revision resets the counter) or expect the live self-heal revert may take longer than ~4s and have the fallback below ready.
* *Show the annotation, not just the tree.*
The `oc get deploy … tracking-id` line is the crisp proof that this came from Git and not from `oc apply` — it turns the abstract "GitOps-managed" into a concrete fingerprint.
* *Have a fallback.*
If the student controller is sluggish (see Watchouts) and a sync is slow live, narrate from the already-Synced `ws solve` end state and the diff view rather than waiting on a fresh sync.

== Top questions attendees ask

*1. "Why can't I just `oc apply` the Application like every other object?"*::
Because the Argo CD instance is *shared* by the whole room, and attendees are not granted write access to its control-plane namespace (`ogsr-student-gitops`) — an `oc apply` there returns `Forbidden`.
You act *through* Argo CD instead, signed in with your OpenShift identity, and its RBAC lets you manage only your own project (`proj-{user}`).
The Application object is identical either way; the UI is just how you create it under your own, scoped identity.
This is the tenancy model that makes one Argo CD safe for many users.

*2. "How fast does self-heal revert drift — is it instant?"*::
Effectively yes: self-heal is *watch-triggered*, so Argo CD reacts to the live change and reverts it in a few seconds (measured ~4s here), not on a slow poll.
The controller's periodic reconcile interval (`appSync: 90s` on this cluster) is only the *ceiling* — the longest a drift could linger if a watch event were ever missed under load.
The manual *Sync* button is also instant, so an attendee cannot out-edit Git: drift is reverted within seconds whether they Sync it or leave self-heal to do it.
That is true for a *fresh* drift against a revision Argo CD has not just synced.
(If a revert ever takes minutes, suspect a *repeat* drift against the same Application and the same Git revision first — Argo CD's own self-heal backoff, not the controller's health — see Watchouts and Troubleshooting.)

*3. "Kustomize or Helm — which should we use?"*::
Both, and Argo CD renders either — an Application's `source` can point at a Kustomize path *or* a Helm chart with values.
Rule of thumb: *Kustomize* when you are layering environment patches over YAML you own (this repo — base + overlays), *Helm* when you are installing a *packaged* app whose author exposes a `values.yaml` contract.
GitOps is agnostic; it reconciles the rendered output regardless of which templating produced it.

*4. "Is deploying straight from Git really how production works?"*::
Yes — that is the whole point.
Promotion becomes *editing Git* (a reviewed, diffable, revertible commit), and a controller reconciles each environment to its folder, so no pipeline needs standing write access to production.
What this module does *not* yet show is *scale*: one Application per environment does not fit dozens of apps.
The next GitOps module generates Applications with ApplicationSets, orders them with sync waves, and adds progressive delivery (Argo Rollouts) — that is where "GitOps in the large" lives.

*5. "What is the difference between Refresh and Sync?"*::
*Refresh* re-reads Git and the live cluster and recomputes *status* (Synced/OutOfSync, health) — it changes *nothing* in the cluster.
*Sync* applies the desired state to the cluster to close the gap.
You Refresh to *see* where things stand (e.g. after a git commit, to make Argo CD notice the new revision); you Sync to *make it so*.
Self-heal is automated Sync on drift; auto-sync is automated Sync on new Git revisions.

== Watchouts

* *Verify the student Argo CD controller has adequate memory.*
The Argo CD application-controller caches the whole cluster's resource tree (across every user namespace and CRD), so it is memory-hungry regardless of how few Applications exist.
At *1&nbsp;Gi* it OOMKilled and crash-looped during the build (even with two apps), which stalled syncs and self-heal; the platform team raised it to *2&nbsp;Gi* with `appSync: 90s`, and the beats have run fast since.
(Whether the OOM alone accounts for the minutes-long self-heal timing seen at the time, or Argo CD's own self-heal backoff — see below — was also a factor, was never distinguished; see Provenance.)
Pre-flight check 2 confirms the controller is `ready` with a stable restart count — if it is crash-looping, escalate to the platform team; it is not something an instructor can fix from the UI.
* *Self-heal is fast (watch-triggered) on a fresh drift — a repeat drift against the same Application and revision backs off.*
Measured ~4s on this cluster for a first drift; the `appSync: 90s` interval is only the ceiling for that case.
A *second* drift against the same Application and the *same* Git revision runs into Argo CD's own self-heal backoff — a smoke pass measured successive waits of 4m48s, 4m20s, 3m2s, 2m8s, 1m17s as `SelfHealAttemptsCount` climbed.
This is documented Argo CD behavior (the `argocd-cm` here carries no override), not a fault.
Confirm it via `Skipping auto-sync: already attempted sync to […] with timeout 0s (retrying in …)` in the application-controller log and `SelfHealAttemptsCount` on the Application, rather than assuming the controller is unhealthy — see Troubleshooting.
If a self-heal revert ever takes minutes, suspect this backoff *first*; the controller's memory or health is the second suspect, not the first.
* *Attendee Applications live in the shared control plane and are created via the UI.*
There is no per-user Argo CD instance and no `oc apply` path: GitOps {gitops_version} refuses per-user source namespaces on a namespace-scoped instance.
Each attendee's Applications live in `ogsr-student-gitops` with `.spec.project: proj-{user}`.
`ws reset`/`ws prep` delete them *by project* (attendees create them in a UI and cannot be relied on to label them), so a reset cleans up a user's apps without touching anyone else's.
* *Argo CD login is OpenShift SSO.*
Attendees click *Log in via OpenShift* (Dex + OpenShift OAuth) and authenticate as their workshop user; Argo CD maps that identity to `role:{user}`, scoped to `proj-{user}`.
If a login lands them with no visible apps, they likely logged in as the local `admin` account instead of via OpenShift — send them back to the *Log in via OpenShift* button.
* *The platform (`openshift-gitops`) instance is separate from the student instance — by design.*
The workshop's own entry-state machinery runs on `openshift-gitops`; attendees use `ogsr-student-gitops`.
A mistake (or an OOM) in the student instance cannot touch the entry states — verified during the build (the platform instance stayed Healthy while the student controller crash-looped).
That failure-domain split is the point of the two-instance topology; do not "consolidate" them.
* *Driving Argo CD from the `argocd` CLI through the route: expect 504 on long calls.*
A blocking `argocd app get --hard-refresh` can exceed the edge route's timeout and return `504` — the refresh still proceeds server-side.
The *UI* refresh is async and unaffected; if you script against the API, use the async annotation (`argocd.argoproj.io/refresh=hard`) rather than a blocking CLI Get.
* *`git clone` in the terminal is anonymous-read; push needs the workshop password.*
Exercise 1 clones read-only (fine without credentials).
Exercise 4's git-driven change is easiest via the *Gitea UI* pencil (a commit with no credential prompt); the terminal `git push` alternative prompts for the attendee's workshop username and password.

== Provenance

GitOps Fundamentals is built fresh on OpenShift GitOps 1.21.1 (Argo CD 3.4, `argoproj.io/v1alpha1`) against the Parasol `claims-config` Kustomize repo, on a shared student Argo CD instance with per-user AppProjects and OpenShift-SSO login, then performed end-to-end on OpenShift 4.21.22 as `user7` on 2026-07-10/11.
Every command, output, and timing — the `oc apply` Forbidden denial, the 3s/26s first sync of seven objects, the drift diff (`replicas: 3` vs `1`), the instant manual-Sync revert, the ~4s watch-triggered self-heal revert, and the git-driven stage promotion (edit `main`, Refresh in ~20s, Sync) — was captured on-cluster.
An earlier build measured self-heal at ~190s; at the time that was attributed to the controller OOMing at 1&nbsp;Gi, and after the platform team raised it to 2&nbsp;Gi (`appSync: 90s`) the revert was re-measured at ~4s across three trials — the number above.
That attribution is now uncertain, not confirmed wrong: a second, independently documented mechanism — Argo CD's own self-heal backoff on a repeat sync to the same Application and the same Git revision — produces waits in the same range (a later smoke pass measured 4m48s down to 1m17s across six backed-off attempts), and the three re-measurement trials were all early attempts against a revision each was the first drift on, so they would read fast under either explanation.
The OOM was real and the memory increase to 2&nbsp;Gi may well have been necessary regardless; what was not done at the time was distinguish "controller OOMing" from "self-heal backoff on a repeated sync" as the cause of the original ~190s reading, and that distinction cannot be reconstructed now.
No content ported from stale material.
