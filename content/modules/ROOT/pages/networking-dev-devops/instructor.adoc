= Networking for Dev & DevOps: Instructor Guide
:module-id: networking-dev-devops
include::partial$version-attributes.adoc[]

// Internal delivery runbook. Timings MEASURED on the build cluster (OCP 4.21.22, OVN-Kubernetes)
// as user1 on 2026-07-13 by performing every exercise. The Gateway API segment (exercise 6) is
// read-only — the GatewayClass state was captured live; a Gateway was deliberately NOT created (it
// stays Programmed=False on this bare-metal cluster and would install a lightweight Istio in
// openshift-ingress). Cluster-domain values in output are scrubbed to the placeholder apps.example.com.

This module has two beats that land, and they bracket the hands-on.
It *opens* on the flat network — a `demo-client` pod that has nothing to do with the app opening a connection straight to the database (exercise 1) — and it *closes* on the reveal that the same pod is now *blocked* while the API still gets through (exercise 4), followed by a partner namespace isolated *natively* by a UserDefinedNetwork with zero policy (exercise 5).
Everything between is the mechanics — Services/endpoints, the exposure tree, default-deny-then-allow — that makes those beats legible.
Optimize the room for exercise 4 (let the "same database, two answers" land) and exercise 5 (let "no policy at all" land).

The single most common confusion is *a DNS egress allow written with `port: 53` that does nothing* — OVN-Kubernetes matches the port against the CoreDNS *pod* (5353), so the rule must use the named ports.
Pre-empt it (see Watchouts); the lab teaches the named-port form directly and explains why.

include::partial$prep-before-you-teach.adoc[]

== Timing

Total hands-on: *~40 min* (add ~15 min of concept up front, ~5 min buffer).

[cols="1,4,1,2",options="header"]
|===
| # | Exercise group | Attendee time | Measured core

| 1 | Map the network and trace a packet | ~4 min | `oc get svc/endpoints` instant; selector break → endpoints `<none>` in *~2 s*; reachability probes instant (TCP connect). Whole group measured at *21 s* of command time
| 2 | Expose the front end to the world | ~11 min | edge Route create → external `curl` succeeds in *~2 s*; NodePort create/inspect/delete instant; LoadBalancer `<pending>` visible immediately; then the re-encrypt beat at *46 s* of command time (26 s of it the single `parasol-web` rollout)
| 3 | Lock the namespace down with default-deny | ~5 min | policy apply instant; `parasol-claims` drops to `0/1` within *~30–70 s* (periodSeconds 10 × failureThreshold 3, plus policy-propagation lag); reading the break is the time cost
| 4 | Re-open only what the app needs | ~10 min | DNS allow → resolves in *~4 s*; db allow pair → `parasol-claims` back to `1/1` in *~10–20 s*; probes instant; the payoff is the point
| 5 | Native isolation with a UDN | ~6 min | all `oc exec` reads; the isolation probe *times out by design* (~6 s); reading + the "no policy" reveal. Whole group measured at *20 s* of command time
| 6 | Gateway API `[INSTRUCTOR-DEMO]` | ~4 min | you perform; all read-only; *6 s* of command time — the budget is your talk track. See the script below
|===

[NOTE]
.How this timing was measured
====
On the build cluster (OCP {ocp_version}, OVN-Kubernetes) on 2026-07-13, every exercise was performed as `user1` and timed.
The commands themselves are near-instant — `oc get`, `oc apply`, `oc patch` all return in about a second.
The genuine waits are: NetworkPolicy taking *~3–9 s* to program the data plane (a probe that should now be blocked keeps succeeding for a few seconds, then starts timing out); `parasol-claims` readiness swinging on its *10 s* probe period (with the default `failureThreshold: 3`, it drops to `0/1` in *~30–70 s* after default-deny — not a flat ~10 s — and returns to `1/1` ~10–20 s after the db allow); and the reachability probes, which *time out on purpose* (the `timeout 5`/`timeout 6` is the wait, and it is the signal).
The *Attendee time* is dominated by reading the breaks and the payoff, not by command latency.
The first pod attach to the partner UDN can take ~30 s on a cold namespace — the entry state already did it, so attendees never wait on it.

The re-encrypt beat closing exercise 2 was added later and measured the same way on 2026-07-25: *46 s* of command time end to end, of which *26 s* is the one `parasol-web` rollout after the certificate is mounted.
The serving-cert Secret appears in *under 5 s* of the annotation.
Its budget came from re-measuring the three lightest groups rather than from the payoff: exercises 1 and 5 measured *21 s* and *20 s* of command time against 5 and 7 minutes of attendee time, and exercise 6 is *6 s* of read-only commands — so each gave up one minute, and exercise 2 absorbed all three.
Exercises 3 and 4 were deliberately left alone: exercise 3 contains a genuine 30–70 s wait you cannot compress, and exercise 4 is the module's payoff.
The total is unchanged at *40 min*.
====

== How to know the room can move on

Two instruments tell you this without guessing, and neither is the clock. Each exercise ends on a `[.checkpoint]` block with a *✔ Verify:* line in the lab — that is the move-on signal, written for attendees to confirm before continuing. For the whole module, `ws verify networking-dev-devops --user <u>` is the same self-check the attendee can run any time; a room reporting all checks green is actually done, not just out of time.

== Pre-flight checklist

Run these *before the session* (as cluster-admin, `KUBECONFIG` on the workshop cluster).

`ws prep networking-dev-devops` waits up to *5 minutes* for the entry state to converge across both namespaces (no module-specific `waitSeconds` is declared, so this is the engine default). It normally returns well inside that — the ceiling is a timeout, not a duration. Attendees start it themselves from the top of the *concept* page, so deliver the concept at its normal pace and it converges under you.

. *A sample user's entry state materializes and verifies.*
+
[source,sh,role=execute]
----
ws start networking-dev-devops --user user1
ws verify networking-dev-devops --user user1 --entry-only
----
+
Expect `✅ all 10 checks passed`.
The checks cover both namespaces: the `{user}-dev` marker + the three tiers (`claims-db` ready, `parasol-web`/`parasol-claims` present) + `demo-client` ready, and the `{user}-partner` UDN + `partner-workload` on it; plus the clean-slate assertions (no default-deny, no Route yet).

. *The parasol app images are present (this cluster has the imagestreams).*
+
[source,sh,role=execute]
----
oc get pods -n user1-dev -l 'app in (parasol-web,parasol-claims)'
----
+
Expect both `1/1 Running`.
If they are `ImagePullBackOff`, the `ogsr-parasol-images` imagestreams were not loaded on this cluster — the lab's app-tier behaviour (the readiness break/recover in exercises 3–4) needs them; `claims-db`, `demo-client`, and the partner UDN run on always-present platform images and work regardless.

. *The verification substrate is up and tooled.*
+
[source,sh,role=execute]
----
oc exec deploy/demo-client -n user1-dev -- sh -c 'command -v curl && command -v openssl && command -v bash'
----
+
Expect three paths printed.
The whole lab leans on `oc exec deploy/demo-client -- bash -c '</dev/tcp/...>'` for its reachability probes; `bash` is required for the `/dev/tcp` pseudo-device.

. *The partner UDN attached (the pod is really on `ovn-udn1`).*
+
[source,sh,role=execute]
----
oc exec deploy/partner-workload -n user1-partner -- ip -brief addr show ovn-udn1
----
+
Expect an `ovn-udn1` interface on a `10.20.x.x` address.
If it is missing, the UDN did not attach before the pod — `ws reset networking-dev-devops --user user1` (the reset re-applies the UDN at sync-wave -1, before the workload).

. *The service CA operator is up (the re-encrypt beat depends on it).*
+
[source,sh,role=execute]
----
oc get clusteroperator service-ca -o jsonpath='service-ca Available={.status.conditions[?(@.type=="Available")].status}{"\n"}'
----
+
.Expected output
[source,texinfo]
----
service-ca Available=True
----
+
This is the operator that mints the serving certificate from the Service annotation.
It is a core cluster operator, not something the workshop installs — if it is not `Available`, the re-encrypt beat cannot issue a certificate and you should skip to exercise 3 rather than debug it live.

. *A sample user can actually perform the re-encrypt beat.*
+
[source,sh,role=execute]
----
oc auth can-i patch service    --as=user1 -n user1-dev
oc auth can-i patch deployment --as=user1 -n user1-dev
oc auth can-i create route     --as=user1 -n user1-dev
----
+
.Expected output
[source,texinfo]
----
yes
yes
yes
----
+
The beat annotates a Service, mounts a Secret into a Deployment, and creates a Route — all three are within the attendee's own namespace role. Checked by impersonation, not assumed.

. *The GatewayClass is Accepted (for the exercise-6 demo) — and attendees can read it.*
+
[source,sh,role=execute]
----
oc get gatewayclass openshift-default
oc auth can-i get gatewayclasses.gateway.networking.k8s.io --as=user1 --as-group=workshop-attendees
----
+
Expect `ACCEPTED=True` and `yes`. The GatewayClass is a cluster-wide singleton set up by the platform; you only read it. The `yes` matters because the lab's exercise-6 block has *attendees* run `oc get gatewayclass` — the read comes from the `platform-observer` ClusterRole (workshop-config ≥0.1.26); a `no` means the workshop-config layer is stale — sync it before the session.

. *A representative attendee command actually works — from the cockpit, as the attendee.*
+
[source,sh,role=execute]
----
ws smoke networking-dev-devops user1     # execs the in-cluster demo-client and proves it carries curl + openssl, as the attendee, in the live cockpit
----
+
This is the only pre-flight instrument that runs *as the attendee*, in the real cockpit terminal — everything above runs as cluster-admin. `demo-client` is the verification substrate this module leans on for every Route/NetworkPolicy/exposure probe; expect all checks to pass.

== Instructor demo — the Gateway API (exercise 6)

All read-only.
*Do not create a `Gateway` live:* on this bare-metal cluster it stays `Programmed=False` (no LoadBalancer to give it an address) and creating one makes the ingress operator install a lightweight Istio in `openshift-ingress` — cruft you do not want mid-session.
Show the *model* and the *boundary* instead.

=== The model is installed and GA

[source,sh,role=execute]
----
oc get gatewayclass
oc get crd | grep -E '^(gateways|httproutes|grpcroutes|referencegrants)\.gateway\.networking\.k8s\.io' | awk '{print $1}'
----

.Expected output — captured on a cluster that also runs the Service Mesh stack; `openshift-default` is the only row this module guarantees
[source,texinfo]
----
NAME                CONTROLLER                           ACCEPTED   AGE
istio               istio.io/gateway-controller          True       3d
istio-remote        istio.io/unmanaged-gateway           True       3d
openshift-default   openshift.io/gateway-controller/v1   True       2d
gateways.gateway.networking.k8s.io
grpcroutes.gateway.networking.k8s.io
httproutes.gateway.networking.k8s.io
referencegrants.gateway.networking.k8s.io
----

IMPORTANT: *Check the list before you promise it.* This module's `stacks:` is empty (`/modules.yaml`) — the `istio`/`istio-remote` rows come from the *Service Mesh* module's stack, and on a cluster provisioned without it you get `openshift-default` alone. Run `oc get gatewayclass` in your pre-flight and adapt the talk track: the one-API-many-implementations beat is a bonus where the rows exist, never a promise.

Talk track: the portable shape is *GatewayClass* (an implementation's advertisement — the platform's is `openshift-default`, `Accepted` on every cluster; where the shared Service Mesh control plane is installed it registers `istio`/`istio-remote` of its own, a nice one-API-many-implementations beat) → *Gateway* (a listener the platform team owns) → *HTTPRoute* (routing rules app teams attach, including `backendRefs` with `weight` for a traffic split). It is GA on OpenShift {ocp_version}; the same three-object model runs on any conformant platform.

=== The honest L4 boundary

[source,sh,role=execute]
----
for c in tcproutes tlsroutes udproutes; do
  oc get crd $c.gateway.networking.k8s.io >/dev/null 2>&1 && echo "$c: PRESENT" || echo "$c: ABSENT (not on the supported channel)"
done
----

.Expected output
[source,texinfo]
----
tcproutes: ABSENT (not on the supported channel)
tlsroutes: ABSENT (not on the supported channel)
udproutes: ABSENT (not on the supported channel)
----

Talk track: Routes and today's Gateway API are HTTP(S)/SNI-centric. The experimental L4 route types are deliberately not installed (a validating admission policy blocks even cluster-admin from adding them), so raw-TCP/passthrough ingress is the *service mesh's* job — the advanced Service Mesh module. This is the same boundary the spec calls out; don't promise attendees L4 ingress via a Route.

=== The bare-metal gap (and the TLS direction)

Explain, do not perform: creating a `Gateway` on `openshift-default` here leaves it `Programmed=False` because no LoadBalancer provider answers — the same reason the exercise-2 `type=LoadBalancer` sat at `<pending>`.
On a cluster *with* a provider (a cloud LB, or MetalLB on bare metal), the Gateway gets an address and the model runs end-to-end.
The TLS direction is a cert-manager-issued certificate ({cert_manager_version}) referenced from the Gateway listener's `certificateRefs` (a self-signed CA issuer for per-user certs, since public ACME is rate-limited at event scale).
The module's `ws solve` ships that whole end state — Gateway + weighted `HTTPRoute` + cert-manager `Certificate` — behind a `gatewayApi=true` flag that is *off by default* precisely because of this gap; flip it only on a cluster that has an LB provider.

== Demo-mode notes

Beyond the Say/Show/Do arc in the demo flavor:

* *Run the demo from the entry state, not `ws solve`.*
The whole story is *building* the controls from a flat network. `ws solve networking-dev-devops` renders the finished end state (policies + Route in place) — use it to *show the destination* or pre-stage, never as the demo's start.
* *Let the break breathe (beat 2).*
After `default-deny`, pause on `parasol-claims` at `0/1` and the DB health-`DOWN` log line. This is the moment that makes "the network is a control plane" concrete — the app didn't crash, the *network* stopped it.
* *Land the payoff line (beat 4).*
"Same database, same namespace, two pods, two answers." Say it while both probe results are on screen — the API `OPEN`, the `demo-client` `BLOCKED`.
* *The UDN reveal needs the `oc get networkpolicy` first (beat 5).*
Show there is *no* policy in the partner namespace *before* the isolation probe, or the punchline ("no policy at all") doesn't register.

== Top questions attendees ask

*1. "My DNS allow uses `port: 53` — why is DNS still broken?"*::
Because OVN-Kubernetes evaluates a NetworkPolicy egress port against the *destination pod*, and the CoreDNS pods listen on *5353*, not 53 (the `dns-default` Service maps `53 → 5353`).
A rule for `port: 53` never matches the pod, so DNS stays denied.
Use the CoreDNS pods' *named ports* `dns` / `dns-tcp` (what OpenShift's own operators do) or the number `5353`.
This was verified live on the build cluster — `53` fails, `dns`/`dns-tcp` works — and it is the single most common self-inflicted "my policy is correct but nothing works."

*2. "Why is my `type=LoadBalancer` Service stuck at `<pending>`? Is the cluster broken?"*::
No — `<pending>` means *no provider answered*, not "failed."
A LoadBalancer Service needs something to hand out external IPs: a cloud load balancer, or an add-on such as MetalLB on bare metal.
This workshop cluster is bare metal with no such provider, so the external IP never arrives.
The fix is a *Route* (which needs no provider) or, if you truly need an L4 external IP, install a provider.

*3. "Why re-encrypt instead of just passthrough? Both encrypt the last hop."*::
Because passthrough costs you the router.
With passthrough the router never decrypts, so it can only see SNI — it cannot route on a URL path, cannot split traffic by path, and cannot add or rewrite HTTP headers (the Route API notes passthrough is incompatible with header actions).
It also pushes certificate management into the application: the pod must hold a certificate the *outside world* trusts, which means real public certificates distributed to workloads.
Re-encrypt keeps the router doing HTTP-layer work *and* encrypts the last hop, and the pod's certificate only ever has to be trusted *inside* the cluster.
Passthrough is the right answer when the backend must terminate TLS itself — client-certificate authentication, or a non-HTTP protocol.

*4. "We never gave the Route a destination CA certificate. Why does it verify the backend?"*::
Because the backend's certificate is *infrastructure-generated*.
The Route API's documentation for `destinationCACertificate` states that when the field is not specified, the router may provide its own destination CA and perform hostname validation using the short service name `service.namespace.svc` — which is exactly the subject the service CA operator issues (`CN=parasol-web.{user}-dev.svc`, which attendees read at hop 2).
So a service serving certificate verifies automatically, with nothing configured.
You *do* need `destinationCACertificate` when the backend presents a certificate from your own CA rather than the cluster's.
Verified live on the build cluster: the reencrypt Route was created with `spec.tls` containing only `{"termination":"reencrypt"}` and was `Admitted=True` and serving.

*5. "I applied default-deny and `parasol-claims` went `0/1` — I didn't touch it. Why?"*::
That is the policy working.
`default-deny-all` denies *egress* too, so `parasol-claims` can no longer open its connection to `claims-db:5432`; its readiness probe checks the database, the check fails, and the pod is marked NotReady (it does *not* crash — status stays `Running`).
Its log shows `Database connections health check … DOWN … Acquisition timeout`.
Add the `parasol-claims → claims-db` egress allow (and the matching db ingress allow) and it returns to `1/1`. This is the intended "app breaks, now write the allows" beat.

*6. "The partner workload still has an `eth0` on the cluster network — is it really isolated?"*::
Yes.
A Primary UDN pod keeps a cluster-network `eth0` for node-level infrastructure (kubelet health checks and the like), but its *default route* leaves through `ovn-udn1` on the UDN's `10.20.0.0/16` segment — that is its primary network.
OVN-Kubernetes enforces the isolation regardless of the extra interface: the pod cannot reach workload pods in other namespaces (verified — the connection to `parasol-web` times out), even though it can still resolve DNS via the shared cluster service.
The presence of `eth0` is an implementation detail, not a leak.

*7. "Can we demonstrate the Gateway API end-to-end here?"*::
Not on this cluster.
The OpenShift Gateway API implementation provisions the Gateway behind a LoadBalancer Service, which — as with question 2 — stays `<pending>` on bare metal, so the Gateway never gets an address and never reaches `Programmed=True`.
Show the *model* (GatewayClass → Gateway → HTTPRoute, weighted splits) and the *boundary* (no L4 route types), and run the end-to-end path on a cluster that has an LB provider. The Route is the runnable exposure here.

== Watchouts

* *The DNS named-port trap (see question 1).*
Attendees who "fix" DNS with `port: 53` will see it stay broken and blame the cluster. The lab teaches `dns`/`dns-tcp` directly with a NOTE explaining why; if someone is stuck, check their `allow-dns-egress` ports first.
* *default-deny breaks DNS and the app — that's the lesson, not a bug.*
Both are expected in exercise 3. Don't let a confused attendee "fix" it by deleting default-deny; the point is to re-open flows deliberately in exercise 4.
* *The Route needs a router-ingress allow under default-deny.*
`allow-web-from-router` (namespaceSelector `policy-group.network.openshift.io/ingress: ""`) is what lets the ingress router reach `parasol-web` once default-deny is on. Without it, the Route returns an error even though the Service is fine — a subtle one to debug if an attendee skips it.
* *The `demo-client` block is over-determined — frame it honestly.*
Under the end state, `demo-client` is blocked both because default-deny denies its egress (it got no allow) *and* because the `claims-db` ingress admits only `parasol-claims`. Present it as "no rule lets it through," not "the db ingress policy alone stops it" — both point the same way, and that is the correct mental model.
* *The re-encrypt beat's real failure mode is a Route pointed at the plaintext port.*
If an attendee creates the reencrypt Route against `--port=http` (8080) instead of `--port=https` (8443), the router tries to open TLS to a plaintext listener and the Route returns *503* with the router's "Application is not available" page — not a TLS error, which is what makes it confusing. Confirmed live. Check `spec.port.targetPort` first; it is `https` when correct.
* *The pod must be serving TLS before the Route will work, and `1/1 Ready` does not prove it.*
The readiness probe checks the plaintext port, so `parasol-web` goes `1/1` whether or not the TLS listener came up. The lab has attendees grep the startup line for `Listening on: … and https://0.0.0.0:8443` for exactly this reason — if someone's re-encrypt Route 503s, check that line before anything else.
* *The two `oc set` commands trigger two rollouts.*
`oc set volume` and `oc set env` each patch the Deployment, so the second supersedes the first mid-roll; `oc rollout status` after both is what settles it (measured 26 s). An attendee who runs `oc rollout status` between them will see it complete twice and may think something is wrong.
* *LoadBalancer `<pending>` and Gateway `Programmed=False` are the same root cause.*
Bare metal, no LB provider. If asked why the Gateway API isn't hands-on, it is this — not a version or licensing gap.
* *Don't create a Gateway live.*
It stays unaddressed *and* triggers a lightweight Istio install in `openshift-ingress`. Keep exercise 6 read-only.
* *The `EndpointSlice` deprecation warning on `oc get endpoints` is cosmetic.*
`oc get endpoints` prints `v1 Endpoints is deprecated … use discovery.k8s.io/v1 EndpointSlice`. It is the clearest view for teaching Service→endpoints; the warning is informational and harmless.

== Provenance

Networking for Dev & DevOps is a net-new module (the spec's "fresh for UDN/GwAPI"), built against this repo's live cluster.
Every command, output, timing, the default-deny app break (the `parasol-claims` DB health-`DOWN` log), the DNS named-port subtlety (`53` fails / `dns`-`dns-tcp` works), the isolation block, the edge Route, the UDN interface and subnet, the NodePort assignment, the LoadBalancer `<pending>`, and the GatewayClass state were captured on OpenShift 4.21.22 as `user1` on 2026-07-13 during the build.
The Gateway API segment is read-only by design (no Gateway created — bare-metal gap + Istio side effect); no content was ported from stale material.

The *re-encrypt* beat closing exercise 2 was added on 2026-07-25 and performed end to end as `user7` on the same cluster before it was written: the service serving certificate minted from the Service annotation (Secret present in under 5 s), the pod's TLS listener (`Listening on: http://0.0.0.0:8080 and https://0.0.0.0:8443`), the two-port Service, the reencrypt Route admitted with `spec.tls` containing *only* `{"termination":"reencrypt"}`, the external `curl` succeeding, and the two-hop certificate capture (`CN=*.apps.<cluster>` at the router, `CN=parasol-web.<ns>.svc` issued by `openshift-service-serving-signer` at the pod).
The 503 failure mode was reproduced deliberately by pointing a reencrypt Route at the plaintext port.
The beat was also checked *against the rest of the lab*: with exercise 3's `default-deny-all` and exercise 4's `allow-web-from-router` in place, both the edge and the re-encrypt Route still return `HTTP 200` (that policy sets no port restriction, so 8443 is covered — exercise 4 needed no change), and `ws verify networking-dev-devops user7` reports *all 14 checks passed* with the beat applied.
The claim that the router auto-verifies an infrastructure-generated backend certificate is grounded in the Route API's own field documentation (`oc explain route.spec.tls.destinationCACertificate`), not recalled.
