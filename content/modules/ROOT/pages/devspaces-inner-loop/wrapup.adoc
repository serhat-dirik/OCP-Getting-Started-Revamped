= Dev Spaces & the Inner Loop: Wrap-up
:module-id: devspaces-inner-loop
include::partial$version-attributes.adoc[]

== What you built

Starting from a one-click factory URL, you opened Parasol's claims service in a browser IDE that started in under two minutes, confirmed it was pinned to JDK 21 by its devfile, ran it in Quarkus dev mode against the *real* in-cluster `claims-db`, added a live endpoint and watched it hot-reload in about two seconds, extended the workspace with a Valkey cache by editing the devfile, pushed your change to your Git fork, attached a debugger to a running request through Quarkus dev mode's live debug port, and reached past the workspace to port-forward the database and ship your change as a running Deployment.

[mermaid]
....
graph LR
  fork[/"your-name/parasol-claims<br/>(fork + devfile)"/] -->|factory URL| ws["Dev Spaces workspace<br/>che-code + Quarkus dev mode"]
  ws -->|"cross-ns DNS"| db[("claims-db<br/>your-name-dev")]
  ws -->|edit + save| reload["hot reload ~2s"]
  ws -->|git push| fork
  fork -.->|"outer loop (next modules)"| pipe["pipeline → GitOps"]
  classDef done fill:#e6f4ea,stroke:#137333,color:#0d652d;
  class ws,reload,db done;
....

// media-pass: recap SVG devspaces-inner-loop-04-what-you-built.svg (green = what the attendee ran this module).

The one idea under all of it: the inner loop moved into the cluster without getting slower.
The environment came from a file in Git, the code talked to real cluster services, and the whole edit-run-observe cycle stayed measured in seconds — with nothing installed on a laptop.

== Map to your org

Take these back to how your teams work today:

. *How long until a new hire ships their first change?*
You went from a link to a running, editable, database-connected service in a couple of minutes.
For a real team of yours: measure the actual elapsed time from "laptop handed over" to "first pull request merged."
How much of that is environment setup — and what would it be worth to make it a link?
. *What does your laptop fleet cost — in money and in drift?*
Every developer laptop is a licensed, secured, backed-up, drifting machine.
Count them; then ask how many of those toolchains genuinely need to be local versus could be a workspace the platform team maintains once, in Git.
. *Where does "works on my machine" still cost you?*
Think of the last bug that turned out to be a version or config difference between a developer's machine and production.
If the toolchain were a reviewed devfile that the pipeline used too, would that class of bug still exist?

== When *not* to do it this way

In-cluster workspaces are a strong default for server-side development, but they are not universal.
Be honest about the edges:

* *Air-gapped or offline development.*
If developers routinely work without cluster connectivity — on aircraft, in secure facilities, at the edge — a browser IDE that lives in the cluster is exactly the wrong tool.
The take-home local options (Podman Desktop, OpenShift Local) fit those cases.
* *Plugins and tooling that need local hardware.*
A workflow that depends on a local GPU, a USB device, a hardware security key reader, or a desktop-only IDE plugin will not fit a browser workspace.
Mobile or desktop app development that needs a local device simulator or emulator is the classic example — those tie the inner loop to hardware and local runtimes a browser workspace does not have.
* *Very heavy native toolchains.*
Some builds (large C++/Rust monorepos, giant asset pipelines) want more local disk and CPU than a shared per-user workspace should hold.
Size the workspace honestly, or keep those builds on dedicated machines.
* *A workspace is not a server.*
Dev mode is for iterating, not for hosting.
Your workspace idles after 30 minutes and is capped at two running at once per user — deliberately, because it is a *development* environment, not a place to run production.

== For your platform team: curate your own workspace templates

You reached this workspace through a factory URL and a devfile.
A platform team turns that into a *ready-to-use template* so developers pick a service from a gallery instead of pasting URLs — and, like everything else on the platform, both levers are code, kept in the GitOps Dev Spaces stack (`platform-portfolio/components/devspaces/`) next to the operator:

. *Workspace defaults and governance* live on the *CheCluster* custom resource — the default editor, the per-user workspace cap, the idle timeout, container-build permissions.
This module ran under exactly such a CheCluster (two workspaces per user, 30-minute idle); changing a field there is one reviewed commit that every developer inherits, not a per-person setting.
. *Ready-to-use samples* — the tiles on the dashboard's *Create Workspace* page — come from a ConfigMap in the `openshift-devspaces` project, labeled `app.kubernetes.io/part-of=che.eclipse.org` and `app.kubernetes.io/component=getting-started-samples`.
Its JSON lists each template's display name, description, tags, and the Git repository (with its devfile) that defines the environment.
Commit that ConfigMap beside the CheCluster, and every developer sees the new template appear.

Because a template is ultimately just a good *devfile in a repository* — the same kind of file you edited in the lab — "adding a workspace template" is publishing that repository and registering it as a sample.
The environment your team standardizes on becomes a reviewed, versioned artifact, not tribal knowledge.

== Go deeper

* *Pipelines Fundamentals & Task Libraries* — the `git push` you just did is where the outer loop begins; the next delivery module turns that push into an automated build-test-deploy pipeline (a separate module in this workshop).
* *Developer Hub & Golden Paths* — instead of forking one repo, scaffold a whole new service — repo, pipeline, GitOps, and a pre-wired Dev Spaces workspace — from one template (a separate module).
* *Config, Secrets & Multi-Environment* — you wired the database with environment variables; that module makes externalized configuration a first-class discipline (a separate module).
* Red Hat and Quarkus documentation: the https://docs.redhat.com/en/documentation/red_hat_openshift_dev_spaces[OpenShift Dev Spaces docs^] (devfile authoring, workspace configuration), and Quarkus's https://quarkus.io/guides/maven-tooling[dev mode, live reload, and remote development^] guide (the `quarkus:remote-dev` hot-reload-against-a-running-Pod flow from exercise 7).

[NOTE]
.Clean up
====
Your workspace idles automatically after 30 minutes of inactivity and resumes where you left off — you do not need to stop it.
To return your world to a fresh claims app + database (and remove your workspace), run `ws prep {module-id} --yes`.
Your Gitea fork keeps the commits you pushed — that is your work, not the module's; delete the fork in Gitea first if you want to redo the lab's code changes from scratch.
Leave everything running if you are continuing to another module — each module materializes its own world.
====
