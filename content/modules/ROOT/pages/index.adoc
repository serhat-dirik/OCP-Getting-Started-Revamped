= OpenShift Application Platform — Getting Started

// Landing page for all three renderings (workshop / demo / instructor).
// Environment values (URLs, users) appear ONLY as attributes — never hardcoded.

Welcome to the *OpenShift Application Platform — Getting Started* workshop.

ifdef::demo[]
[NOTE]
====
*Demo guide.*
Presenter blocks — timing, talk track (Say), on-screen cues (Show), and click paths (Do) — render inline throughout each module.
====

== Demo paths

Three composed agendas, assembled from demo arcs that already exist in this guide.
Every figure below is the sum of that arc's own `⏱` beat chips — read off the pages, not estimated — and the arithmetic is printed so you can check it against the arc before you commit to a slot.

Read each leg's *Before you present* block first: it names the `adm` command that stages that module's world, and some of them are slow — `adm solve gitops-fundamentals` blocks four to six minutes by design.
One runs the other way: xref:agentic-ai/lab.adoc[Agentic AI]'s opening beat needs the deliberately *weak* grounding prompt the entry state ships, so its prep block tells you *not* to run `adm solve` first — solve strengthens the prompt and takes the beat away.

=== Path 1 · Executive briefing — 16 min of beats

Fits a 20-minute slot with about four minutes for the open and questions.
The story is one sentence: the platform runs my app, keeps it running, is governed by Git, and can carry AI.

[cols="4,2,5",options="header"]
|===
| Leg | Beat chips | What lands

| xref:platform-orientation/lab.adoc[Platform Orientation] — beats 1–3
| 2 + 3 + 2 = *7 min*
| A container image becomes a running, three-replica app in the console. No ticket, no server.

| xref:gitops-fundamentals/lab.adoc[GitOps Fundamentals] — beat 2
| *4 min*
| Someone edits the live cluster by hand; Argo CD flags it as drift and puts the diff on screen.

| xref:agentic-ai/lab.adoc[Agentic AI on OpenShift] — beat 1
| *5 min*
| A model that *describes* a tool call becomes one that *makes* it, and the change is one ConfigMap.

| *Total*
| *16 min*
|
|===

[NOTE]
====
GitOps Fundamentals beat 2 ends on the *diff*, not on the revert — the cluster is still drifted when the beat closes.
Either say the revert line and move on, or run beat 3 as well (*+3 min*, total 19) and let the room watch Git win.
====

=== Path 2 · Developer platform — 36 min of beats

Fits a 45-minute slot with about nine minutes for the open, two transitions and questions.
Three arcs, run whole and in order: source becomes an app, the app gets a delivery path, and Git becomes the thing that decides what runs.

[cols="4,2,5",options="header"]
|===
| Leg | Beat chips | What lands

| xref:platform-orientation/lab.adoc[Platform Orientation] — whole arc
| 2 + 3 + 2 + 2 + 3 = *12 min*
| Deploy from an image, scale it, kill a pod and watch it heal, then ask the in-console assistant why a pod restarts.

| xref:build-deliver/lab.adoc[Ways to Build & Deliver Apps] — whole arc
| 3 + 3 + 2 + 3 = *11 min*
| Source-to-Image turns a Git repo into a running, database-backed API — no Dockerfile anywhere.

| xref:gitops-fundamentals/lab.adoc[GitOps Fundamentals] — whole arc
| 3 + 4 + 3 + 3 = *13 min*
| Drift the cluster by hand, revert it with one click, then turn on self-heal and watch it revert itself.

| *Total*
| *36 min*
|
|===

[NOTE]
====
Do not trim the middle of *Ways to Build & Deliver Apps*: beats 2 and 3 are deliberately sized to cover the four-to-five-minute S2I build, and that arc's own fallback note is "never stall on a progress bar".

Have a full hour? Add xref:observability-health-scale/lab.adoc[Observability, Health & Scale] whole (*15 min*, total 51) as the fourth act.
That arc is the reason this path is three modules rather than four — 51 minutes of beats does not fit a 45-minute slot, before a single transition.
====

=== Path 3 · AI and trust — 27 min of beats

Fits a 45-minute slot with about eighteen minutes spare, and you will use them: this is the track that draws the most questions.
Two arcs, both whole — the first shows an agent grounded in your own systems, the second shows what stops it.

[cols="4,2,5",options="header"]
|===
| Leg | Beat chips | What lands

| xref:agentic-ai/lab.adoc[Agentic AI on OpenShift] — whole arc
| 5 + 5 + 5 = *15 min*
| Grounding is engineered; a citation is a claim and a tool call is a receipt; "not found" is an answer.

| xref:ai-assisted-development/lab.adoc[AI-Assisted Development] — whole arc
| 4 + 4 + 4 = *12 min*
| An assistant diagnoses a broken app read-only, earns a scoped write, then is refused by RBAC next door.

| *Total*
| *27 min*
|
|===

[NOTE]
====
Run the second arc whole rather than jumping to its closing beat: the RBAC denial only lands because the room has just watched the *same* agent succeed under a grant you gave it.

Open xref:agentic-ai/instructor.adoc[Agentic AI · Presenter notes] before the room.
The questions this track draws — hallucination, prompt injection, cost per request, "did we just hack the cluster?" — are answered there with measurements, and they are not in the arc.
====

Each module's Concept page carries a presenter framing block sized at five to six minutes.
If the slot has room, open a path with its first module's framing; otherwise go straight to the arc.
endif::demo[]

ifdef::instructor[]
[NOTE]
====
*Instructor guide.*
Each module adds a delivery runbook: measured timing, a pre-flight checklist, the top questions attendees ask, and watch-outs.
====
endif::instructor[]

== About this workshop

This is a hands-on OpenShift enablement workshop, built as a library of self-contained, story-driven modules.
It exists to take you from "I have credentials to a cluster" to "I can develop, deliver, and operate applications on OpenShift — and I understand *why* the platform works the way it does."
Every module is a lab you perform on a real cluster, not a slideshow: you deploy, break, fix, and observe things with your own hands.

By the end of the modules you work through, you will be able to deploy and scale an application, build and secure a delivery pipeline, adopt GitOps so that Git is the source of truth, promote a release safely across environments, protect an app with real identity, and troubleshoot it with metrics, logs, and traces.
The advanced modules go further — service mesh, serverless, eventing, resilience, application modernization, and running AI on the cluster — so you also leave knowing *when* to reach for each capability, and when not to.

== The Parasol story

Parasol Insurance has sold home and auto policies for decades, and its claims platform — the software adjusters use all day to open, assess, and settle a claim — *is* the business.
That platform grew up as a hand-tended application on servers that individual engineers named, patched, and nursed through the night.
It still runs, but every change is a gamble: deployments are manual and no two are quite alike, nobody can say exactly how the app is built, and the one team that guards the servers is a bottleneck for every other team.

The pain is equal parts technical and commercial.
There is no repeatable build and no shared record of how the service ships, so releases risk downtime and security gets bolted on late.
Meanwhile the business wants to move faster — new insurance products, an audit trail that satisfies regulators, and AI to help triage a growing claims backlog — and every one of those ambitions stalls against a delivery process nobody trusts.

You join Parasol's engineering organization to change that, and across the workshop you take the claims platform from its first container to a governed, observable, AI-assisted service on Red Hat OpenShift.
You begin with the *foundations*: deploy the claims app, scale and self-heal it, configure it across environments, and give its data a safe home.
You build the *delivery and trust* layer next — pipelines that ship the same way every time, a software supply chain you can prove, GitOps that makes Git the source of truth, and apps secured with real identity.
You take the *platform and tenancy* view after that, with many teams sharing one cluster under networking, scheduling, and image governance you design and defend.
Finally you reach for the *advanced* capabilities on Parasol's roadmap, from service mesh and serverless to modernization and the AI claims assistant that started the whole conversation.

Every module solves the next real problem Parasol hits, so each platform capability arrives as the answer to a question you already have — not a feature tour, but a story you build.

== How to use this workshop

This workshop is a *library of self-contained modules*, not a single track you must finish end to end.
Two things are true at the same time, and both matter.

*Any module can be your first.*
You never need to have completed an earlier module to start one.
When you begin a module, its world — namespaces, applications, Git repositories, pipelines, and access — is materialized for you on demand, so nothing depends on "please finish the previous module first."

*Following the numbers is still the recommended way through.*
The modules are numbered in teaching order: they build you up from foundations (deploy, scale, configure) toward more advanced topics (pipelines, platform tenancy, service mesh, AI), and each one assumes the *ideas* the earlier numbers taught — even though it never assumes their leftover cluster state.
If OpenShift is new to you, work through your session's modules in ascending order.
If you came for one specific topic, jump straight to it; it will stand on its own.

Your instructor selects the set of modules that fits your session — work through those, lowest number first.

// ORIENTATION IS FLAVOR-SPLIT, and it has to be. The attendee cockpit (showroom.yaml) ships a
// ttyd terminal logged in as {user} with click-to-run; the SA-Demos cockpit
// (gitops/workshop-config/templates/showroom-demos.yaml, header design point 1) deliberately ships
// NO terminal — no ttyd container, no terminal-prep init step, no home PVC — and injects
// `user: your-user`, a non-identity placeholder, instead of an attendee name. This page renders
// verbatim into both, so a single orientation told the SA about a live shell, click-to-run and a
// sign-in as user1: three things that are all false on the cockpit serving it. Keep the two
// versions in step with that template's tab list and its `user` value, not with each other.
ifndef::demo[]
=== Getting around the cockpit

Everything you need is on this screen: there is nothing to install and nothing to set up on your own machine.

Instructions:: The panel you are reading right now, on the left.
Terminal:: A live shell beside these instructions, already connected to the cluster as your user.
When a command shows a *run* control, it is click-to-run — click it and it executes in the terminal, so you never have to retype it (you can still type your own commands there any time).
Some modules give you *two* terminals so you can watch one thing while you do another: kill a pod in one and watch it heal in the other, or tail a pipeline's logs while it runs.
Web console & Gitea:: The OpenShift *web console* and *Gitea* — your Git server, holding your personal copies of the Parasol repositories — open as their own browser tabs, already signed in as you.
Links in the instructions deep-link you to the exact view you need.

You move among these constantly: read a step here, run it in the terminal or click through the console, then confirm the result.
A runnable command looks like the one below — click it, and the terminal prints your username:

[source,sh,role=execute]
----
oc whoami
----

That click-to-run loop is how you work through the entire workshop.

=== Prepare your environment

Each module's lab opens with a one-line command that prepares your environment for that module:

[source,sh,role=execute]
----
ws prep platform-orientation
----

`ws prep` checks your project, tells you if anything is left over from a module you ran earlier, and *asks before wiping* anything to set up the current module's world — so it is safe to run at any time.
Two companion commands help while you work: `ws verify platform-orientation` confirms your environment matches what the module expects, and `ws reset platform-orientation` returns it to a clean starting point.

=== Signing in to the platform tools

Most of your work happens in the cockpit's terminal and the OpenShift console, which are already signed in as you.
A few modules open a separate tool — Dev Spaces, Argo CD, Developer Hub — and those ask you to sign in through OpenShift.

include::partial$idp-choice.adoc[]
endif::demo[]

ifdef::demo[]
=== Getting around the demo cockpit

This is the *presenter* cockpit, and it is not the attendee one.
It is *instructions plus tool tabs*, by design.

Instructions:: The panel you are reading, on the left. The navigation lists three pages per module: the *Concept* framing, the *Demo arc* you present from, and the *Presenter notes* — the instructor runbook, with measured timings, the questions this material draws, and the watch-outs that only bite live.
Tool tabs:: External launchers only — the OpenShift *web console*, *Gitea*, *Dev Spaces*, *Argo CD*, *Developer Hub* and *SonarQube*, each opening in its own browser tab. The cockpit itself is not signed in to anything, so a tool asks for its own sign-in unless your browser already carries a session.
No terminal:: There is no shell on this screen. Run every `Do::` command from *your own terminal*, logged in to the cluster yourself, on the display you are sharing. Where a command block in this guide shows a *run* control, it is inert here — there is nothing behind it to run it in.

Set that terminal's font large before the room can see it: several arcs turn on a single line of output.

[IMPORTANT]
.The identity in these pages is a placeholder, not an account
====
This cockpit has no attendee session to name, so it renders a non-identity: commands and namespaces come out as `your-user`, and a local preview build of this guide falls back to `user1` — a live attendee slot on any installed cluster.
Neither one is you.

Substitute the identity you are presenting as everywhere it appears — namespaces such as `{user}-dev` included — and read the *whole* block before you paste: several arcs mix a substituted literal with an `$NS`-style shell variable you set yourself, inside a single beat.
Get that wrong and you act on somebody else's namespace while the demo looks fine.
====

=== Before you present

Every demo arc opens with a *Before you present* block: the `adm` command that stages that module's world, what to have on screen, which beat is the money moment, and — where it has been measured — how long the staging blocks.
Read it the day before rather than the hour before; some stage cluster state that takes minutes to settle, and a few tell you which `adm` command *not* to run.

If a tool offers a choice of sign-in methods, pick *`workshop-users`* and use the attendee account you are presenting as.
Two front doors never touch OpenShift's login: *Developer Hub* opens on a *Guest / GitHub* chooser where *Guest* is the one click, and *Gitea* asks in its own form.
endif::demo[]

== Module library

Entitlement markers: `[ADS]` = Advanced Developer Suite; `[ADD-ON]` = separate subscription (always optional and flagged).
Everything unmarked runs on components an OpenShift subscription already includes.

=== A — Foundations

[cols="1,3,6",options="header"]
|===
| ID | Module | What you do

// Rows are guarded by ifndef::module-<slug>-hidden[] — undefined (so all render) in CI/preview and
// in an all-modules delivery; set per disabled module in the attendee showroom build (see nav-workshop.adoc).
ifndef::module-platform-orientation-hidden[]
| 1 | Platform Orientation & First App | Deploy Parasol's web front end, scale it, heal a killed pod, publish it with a Route, and read logs in both the console and the CLI.
endif::module-platform-orientation-hidden[]
ifndef::module-build-deliver-hidden[]
| 2 | Ways to Build & Deliver Apps | Deploy a ready image, build from Git source with S2I, or bring your own Dockerfile — then add a database and a polyglot notifications service.
endif::module-build-deliver-hidden[]
ifndef::module-devspaces-inner-loop-hidden[]
| 3 | Dev Spaces & the Inner Loop | Move your IDE into the cluster, hot-reload Quarkus against live services, and hand a teammate your exact environment with a devfile.
endif::module-devspaces-inner-loop-hidden[]
ifndef::module-config-multienv-hidden[]
| 4 | Config, Secrets & Multi-Environment | Promote one image dev → stage → prod with ConfigMaps, Secrets, and liveness/readiness probes that actually gate traffic.
endif::module-config-multienv-hidden[]
ifndef::module-storage-stateful-hidden[]
| 5 | Storage & Stateful Apps | Lose data once, deliberately, then never again: PersistentVolumeClaims, StorageClasses, access modes, and StatefulSets done properly.
endif::module-storage-stateful-hidden[]
ifndef::module-jobs-batch-kueue-hidden[]
| 6 | Jobs, Batch & Queued Workloads | Run one-shot and Indexed Jobs, suspendable CronJobs, and quota-aware batch with Kueue — plus an AI batch-inference run.
endif::module-jobs-batch-kueue-hidden[]
|===

=== B — Delivery & Trust

[cols="1,3,6",options="header"]
|===
| ID | Module | What you do

ifndef::module-pipelines-fundamentals-hidden[]
| 7 | Pipelines Fundamentals & Task Libraries | Build, test, and deploy with Tekton and Pipelines-as-Code, then write and publish a reusable task to Parasol's company library.
endif::module-pipelines-fundamentals-hidden[]
ifndef::module-app-security-testing-hidden[]
| 8 | Application Security Testing (SAST · SCA · DAST) | Run a DevSecOps pipeline end to end — SAST with SonarQube, SCA with Trivy, an RHACS image scan, Tekton Chains signing, and a live DAST scan with ZAP — gating the release on what each stage finds.
endif::module-app-security-testing-hidden[]
ifndef::module-trusted-supply-chain-hidden[]
| 9 `[ADS]` | Trusted Software Supply Chain | Fail a build on a seeded CVE, sign and attest images, generate an SBOM, and watch the cluster refuse to run an unsigned image.
endif::module-trusted-supply-chain-hidden[]
ifndef::module-gitops-fundamentals-hidden[]
| 10 | GitOps Fundamentals | Make Git the source of truth: create an Argo CD Application, drift it live in the console, and watch the platform revert you.
endif::module-gitops-fundamentals-hidden[]
ifndef::module-gitops-at-scale-hidden[]
| 11 | GitOps at Scale & Progressive Delivery | Generate environments with ApplicationSets, order syncs with waves, and auto-roll-back a bad canary on real metrics.
endif::module-gitops-at-scale-hidden[]
ifndef::module-developer-hub-golden-paths-hidden[]
| 12 `[ADS]` | Developer Hub & Golden Paths | Scaffold a new microservice — repo, pipeline, GitOps app, and workspace pre-wired — from one golden-path template.
endif::module-developer-hub-golden-paths-hidden[]
ifndef::module-observability-health-scale-hidden[]
| 13 | Observability, Health & Scale | Find the slow request with metrics, logs, and a distributed trace; autoscale under load with HPA; survive a node drain.
endif::module-observability-health-scale-hidden[]
ifndef::module-securing-apps-keycloak-hidden[]
| 14 | Application Logging | Make your app's logs answer questions: structured JSON, a level you change without a rebuild, credentials that never reach a log line, and one request followed across three replicas.

| 15 | Securing Apps with Keycloak | Protect the app with OIDC login and the API with bearer tokens using Red Hat build of Keycloak, then exchange tokens safely.
endif::module-securing-apps-keycloak-hidden[]
|===

=== C — Platform & Tenancy

[cols="1,3,6",options="header"]
|===
| ID | Module | What you do

ifndef::module-multi-tenancy-workload-security-hidden[]
| 16 | Multi-Tenancy & Workload Security | Host many teams on one cluster: identity, RBAC you design and audit, quotas you exceed on purpose, and Pod Security / SCCs.
endif::module-multi-tenancy-workload-security-hidden[]
ifndef::module-networking-dev-devops-hidden[]
| 17 | Networking for Dev & DevOps | Trace the packet: Services, Routes versus Gateway API, default-deny NetworkPolicies you break and fix, and egress control.
endif::module-networking-dev-devops-hidden[]
ifndef::module-deployment-targets-scheduling-hidden[]
| 18 | Deployment Targets & Scheduling | Tell the scheduler what you need — node selectors, taints, affinity, topology spread, and PodDisruptionBudgets.
endif::module-deployment-targets-scheduling-hidden[]
ifndef::module-registry-images-catalog-governance-hidden[]
| 19 | Registry, Images & Catalog Governance | Manage where images live and who decides: the internal registry, ImageStreams, mirroring, and cluster registry policy.
endif::module-registry-images-catalog-governance-hidden[]
|===

=== D — Advanced Electives

[cols="1,3,6",options="header"]
|===
| ID | Module | What you do

ifndef::module-service-mesh-advanced-gateways-hidden[]
| 20 | Service Mesh 3 & Advanced Gateways | Fix a cascading failure with Service Mesh 3 (Istio/Sail): mTLS, a live Kiali graph, traffic shifting, and advanced ingress.
endif::module-service-mesh-advanced-gateways-hidden[]
ifndef::module-serverless-zero-to-hero-hidden[]
| 21 | Serverless Zero-to-Hero | Scale to zero and back with Knative Services, split revision traffic instantly, and leave with an honest decision matrix.
endif::module-serverless-zero-to-hero-hidden[]
ifndef::module-eventing-deep-dive-hidden[]
| 22 | Eventing Deep-Dive & Serverless Workflows | Decide broker versus channel, route with trigger filters, and orchestrate a claims-approval workflow with Serverless Logic.
endif::module-eventing-deep-dive-hidden[]
ifndef::module-resilience-multicluster-dr-hidden[]
| 23 | Resilience, Multi-Cluster & DR | Restore a deleted namespace with OADP, link two sites with Service Interconnect, and run a DR-tier tabletop.
endif::module-resilience-multicluster-dr-hidden[]
ifndef::module-app-modernization-hidden[]
| 24 | Application Modernization (MTA + Lightspeed) | Assess a legacy Java EE claims app with Migration Toolkit for Applications 8, fix blockers with AI help, containerize, deploy.
endif::module-app-modernization-hidden[]
ifndef::module-agentic-ai-hidden[]
| 24 `[ADD-ON]` | Agentic AI on OpenShift | Build Parasol's claims assistant with a model endpoint, RAG, on-cluster MCP tools, and guardrails — then ship it like any app.
endif::module-agentic-ai-hidden[]
ifndef::module-ai-assisted-development-hidden[]
| 25 `[ADD-ON]` | AI-Assisted Development on OpenShift | Connect a coding assistant through a least-privilege MCP ServiceAccount and watch RBAC deny it cross-namespace.
endif::module-ai-assisted-development-hidden[]
ifndef::module-packaging-distributing-hidden[]
| 27 | Packaging & Distributing Your App | Author a Helm chart as an OCI artifact, dissect a live operator's OLM bundle, and settle template-vs-helm-vs-operator.
endif::module-packaging-distributing-hidden[]
|===

The instructor guide flags the few sequencing constraints; otherwise any subset works — that is the design.

// Footer content-currency stamp (reworded 2026-08-14: the prior "Site build <sha> ·
// <utc>" wording read as a CI-run timestamp and caused three false "stale site"
// reports — see pages-preview.yml for the incident notes). `build-info` is set only
// by the CI/preview builds (site-*.yml); the in-cluster cockpit build leaves it
// unset, so guard it — an undefined attribute must render nothing, never the
// literal "{build-info}".
ifdef::build-info[]
---

_{build-info}_
endif::[]
