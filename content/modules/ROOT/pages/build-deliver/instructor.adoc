= Ways to Build & Deliver Apps: Instructor Guide
:module-id: build-deliver
include::partial$version-attributes.adoc[]

// Internal delivery runbook. Timings measured on the build cluster (OCP {ocp_version})
// as user2 on 2026-07-09; see the "how timing was measured" note below.

This module turns "here is some source code" into a running, database-backed application four different ways.
The one idea that must land is the *build spectrum*: Source-to-Image (S2I) is the fast default, the Dockerfile strategy is a deliberate choice you make when you need control, and knowing which to reach for is the skill.
The single biggest pacing risk is the *first S2I build*: ~4–4.5 minutes on a warm cluster, but as long as *~11 minutes* for the very first build on a *fresh* cluster (cold Maven Central + no builder-image/layer cache). Pre-warm before a live session, and plan the room around it rather than watching a progress bar.

== Timing

Total hands-on: *~50–58 min* (spec target 75 min), leaving room for ~15 min of concept up front.

[cols="1,4,1,2",options="header"]
|===
| # | Exercise group | Attendee time | Measured core (as user2, {ocp_version})

| 1 | Import claims from Git (S2I) | ~15 min | first S2I build (java-21) → `Complete` *~4.5 min warm, up to ~11 min cold on a fresh cluster* (Maven download + Quarkus augmentation), then push
| 2 | The Dockerfile way + compare | ~10 min | Docker build of the repo `Dockerfile` *~95 s warm, up to ~11 min cold* (its multi-stage build also pulls the full Maven tree the first time)
| 3 | Polyglot notifications (Node.js) | ~10 min | Node S2I build *~45 s* (zero dependencies, no npm registry)
| 4 | Browse catalog + deploy database | ~10 min | PostgreSQL 15 rollout *~15–20 s*
| 5 | Wire claims to its database | ~8 min | rollout to Ready *~15 s*; Route answers 200 immediately
|===

[NOTE]
.How this timing was measured
====
The *Measured core* column is real: each step was executed as a workshop user (`user2`) on the build cluster (OCP {ocp_version}) on 2026-07-09 and timed.
The claims S2I build was measured at the namespace's *default* memory limit (1 Gi from the workshop LimitRange); the current `java-21` (UBI 9 / OpenJDK 21) builder completes there without tuning.
The *Attendee time* adds reading, console navigation, and checkpoint overhead on top of that core.
The dominant variable is the *first* claims build — a cold node with no cached Maven artifacts or builder image runs longer; pre-warm before a demo (below).
====

== Pre-flight checklist

Run these *before the session*.
All should pass.

. *Entry state materializes and verifies* for a sample user:
+
[source,sh,role=execute]
----
ws start build-deliver --user user1
ws verify build-deliver --user user1 --entry-only
----
+
Expect `✅ all 6 checks passed` (namespace, entry marker, quota, both Gitea forks, PostgreSQL template).
The entry state's fork job runs in the `gitea` namespace; if a fork is missing, re-running `ws start` re-forks idempotently.

. *The Java 21 builder is present* in the shared catalog:
+
[source,sh,role=execute]
----
oc get is java-21 -n openshift -o jsonpath='{.spec.tags[?(@.name=="latest")].from.name}{"\n"}'
----
+
Expect `registry.access.redhat.com/ubi9/openjdk-21:latest`.
This is the workshop-provided builder the lab imports with; without it, the *Java 21* tile attendees are told to pick is absent and `oc new-app java-21~…` fails (see watchouts).

. *The Parasol PostgreSQL template is present and attendee-readable:*
+
[source,sh,role=execute]
----
oc get template parasol-postgresql-ephemeral -n openshift
----
+
Expect one row.
Attendees read this template from the `openshift` namespace; it renders a Deployment on `postgresql:15-el9`.

. *The `parasol-claims` fork carries a `Dockerfile`* (exercise 2's Docker-strategy build auto-detects it):
+
[source,sh,role=execute]
----
curl -sf -o /dev/null -w '%{http_code}\n' \
  "https://$(oc get route gitea -n ogsr-gitea -o jsonpath='{.spec.host}')/user1/parasol-claims/raw/branch/main/Dockerfile"
----
+
Expect `200`.
The source repo ships both `Dockerfile` and the identical `Containerfile`; if this `404`s, the app-repo seed predates the `Dockerfile` — re-sync the mirror and re-seed (`ws git-refresh`) so forks carry it, or exercise 2 falls back to naming the `Containerfile` path (troubleshooting).

. *A user can build* (warms the builder image and Maven cache on a node):
+
[source,sh,role=execute]
----
oc new-app java-21~$(oc get route gitea -n ogsr-gitea -o jsonpath='{.spec.host}')/user1/parasol-claims.git --name=preflight -n user1-dev
oc logs -f bc/preflight -n user1-dev   # wait for "Push successful"
oc delete all -l app=preflight -n user1-dev
----
+
Expect a `Complete` build ending in `Push successful`.
If the build fails to clone, check the fork exists; if it OOMs, see watchouts (build memory).

== Demo-mode notes

Beyond the Say/Show/Do arc in the demo flavor:

* *Pre-stage the database and pre-warm the build — delete nothing.*
Run `ws solve build-deliver --user {user}` before the session: the pre-staged `parasol-claims` stays running as your fallback, and the live import builds *beside* it under the name `claims-live`.
A freshly built app crash-loops until it is wired to the database (the image's default DB host is deliberately wrong) — the demo's final beat runs `oc set env deploy/claims-live --from=secret/claims-db`, and *that* is what brings it up.
Pre-warming also caches the builder image and Maven artifacts, cutting the live build from its cold ~11 min (or a warm ~4.5) toward ~2.
* *Talk through the build, do not watch it.*
The catalog tour and the `registry.access` vs `registry.redhat.io` trusted-content beat are timed to fill the build.
Start the import, then immediately pivot to the catalog — do not narrate a progress bar.
* *The trusted-content contrast is the intellectual payoff.*
Anonymous UBI pull (200) versus authenticated `registry.redhat.io` (401) is a concrete, memorable proof of "provenance is real."
Have the one-liner ready in a terminal; it lands better than a slide.
* *Keep the demo to import → catalog → trusted content → running app.*
Resist doing the Dockerfile comparison and the polyglot import live — those are the hands-on payoff, and they blow the time budget.

== Top questions attendees ask

*1. "S2I or Dockerfile — how do I choose?"*::
Default to S2I: you write nothing, the platform builds your source with a supported, patched builder image, and you own no `Containerfile`.
Reach for the Dockerfile strategy only when you need something S2I cannot give — a specific or hardened base image, a system package, an unusual build step.
The test is "do I have a *reason* to own a base image?"
If not, S2I.

*2. "The build succeeded but my claims Pod is in CrashLoopBackOff — did the build fail?"*::
No — the build succeeded; the *app* cannot start yet.
`parasol-claims` creates its schema and loads seed data at boot, so it needs a reachable PostgreSQL.
Until you deploy the database (exercise 4) and wire it in (exercise 5), the app exits and restarts.
This is deliberate — it is the module's break-and-fix, and it previews how config and dependencies work in the next module.

*3. "Why is the builder called `java-21` and not just `java`?"*::
The stock OpenShift samples ship a `java` builder stream that trails the current release.
This workshop provides its own `java-21` builder (UBI 9 / OpenJDK 21) so everyone builds on a *current* JDK that matches the Quarkus {quarkus_version} apps.
That is a live example of a platform team curating builder images — the "golden path" idea a later module builds on.

*4. "Where do the builds actually run, and what stops them eating the cluster?"*::
Each build runs in a *build Pod* in the attendee's own namespace, subject to that namespace's ResourceQuota and LimitRange.
The default container memory limit (1 Gi) is enough for these Quarkus builds on the `java-21` builder; a larger app can need more (see watchouts).
Builds are first-class, quota-bound workloads — not magic happening off to the side.

*5. "Is this database real? Where does my data go?"*::
It is a real PostgreSQL 15, but *ephemeral* — it uses an `emptyDir`, so restarting the Pod loses all data.
That is intentional: it keeps the module simple and sets up the storage module, which gives the database durable, identity-stable storage.
Never point anything you care about at an ephemeral database.

== Watchouts

* *The console can't introspect the in-cluster Git repo — and that is expected.*
In *Import from Git* the console shows "repository is unreachable" / "Unable to detect the import strategy" for the Gitea forks, because the console backend cannot reach the in-cluster Gitea host — the repo is public and the build clones it fine, only the console's *inspection* fails.
Attendees must pick *Import Strategy → Builder Image → Java 21* (or *Node.js*) and type the *Name* themselves; the lab and troubleshooting both cover it.
Tell the room up front that they choose the builder by hand — exactly what the CLI's `java-21~<repo>` does — so the warning does not read as "my fork is broken."
(This is a candidate platform fix — console-to-Gitea reachability — flagged to the platform team.)
* *First build is the slow step.*
The initial claims S2I build runs ~4–4.5 min on a warm cluster (Maven dependency download + Quarkus augmentation) at the default memory — but the *very first* build on a fresh cluster is much slower, ~11 min (cold Maven Central, no builder-image or layer cache). Pre-warm with `ws solve build-deliver` and set expectations accordingly.
Set expectations, and use the wait for the catalog/trusted-content material rather than silence.
* *Build memory and the LimitRange.*
Build Pods inherit the namespace LimitRange default (1 Gi).
The current `java-21` (UBI 9) builder completes there; note for your own awareness that the *older* stock `java` stream (OpenJDK 17, UBI 8) has hit `OutOfMemoryError` under that 1 Gi default on this same source, and a larger app can too.
The fix is `oc patch bc/<name> --type=merge -p '{"spec":{"resources":{"limits":{"memory":"3Gi"}}}}'` then rebuild.
(This is also a nice governance teaser — the platform team owns both the builder images and the default limits.)
* *Exercise 2 leads with a working `Dockerfile`.*
The `parasol-claims` repo now ships both a `Dockerfile` and the identical vendor-neutral `Containerfile`, so `oc new-app --strategy=docker` auto-detects the `Dockerfile` and builds with no path to set — the exercise is a clean happy path.
The old "refuses because it is named `Containerfile`" behavior is now a documented *fallback* (lab NOTE + troubleshooting) for repos that ship only a `Containerfile`: name it with `dockerStrategy.dockerfilePath: Containerfile`.
* *The exercise-5 topology grouping is deliberate.*
The exercise ends by labelling the two S2I workloads `app.kubernetes.io/part-of=parasol` and annotating `parasol-claims` with `app.openshift.io/connects-to=["claims-db"]`.
Before that, Topology looks scattered — the catalog template groups `claims-db` under a *parasol* application, but `oc new-app` leaves the two services ungrouped and nothing connects claims to its database, which surprised early testers.
The two commands make Topology match the recap diagram and teach how `part-of` (grouping) and `connects-to` (the connector arrow) work; they touch only labels/annotations, so `ws verify` is unaffected.
* *Claims crash-loops until wired.*
Between exercises 1 and 5 the `parasol-claims` Pod is red in Topology (and briefly `claims-docker` too, in exercise 2).
Tell attendees up front that this is expected and gets fixed at the end, or the noise reads as "broken."
* *Per-user build concurrency.*
When a whole room starts the claims build at once, node CPU and the internal registry take the load; builds queue and run slower than the solo timing.
For large rooms, stagger the start of exercise 1 or pre-warm the builder image on the worker nodes.
* *Gitea fork must exist.*
The lab builds from `{user}/parasol-claims` and `{user}/parasol-notifications`.
If a fork is missing, `ws start build-deliver --user <user>` re-runs the fork job (idempotent).

== Provenance

The Source-to-Image narrative (source → builder image → running app, and the S2I-versus-Dockerfile framing) is adapted from Red Hat's *OpenShift starter-guides* S2I walkthrough (credited in the repo-level `CREDITS.md` per the reuse policy).
All technical steps, the Parasol applications, the PostgreSQL catalog template, and the build flows are freshly built and verified on OpenShift {ocp_version} (2026-07-09).
No content ported from the 2020-era labs.
