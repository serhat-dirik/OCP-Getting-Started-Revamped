= MNN — <Module Title>: Lab
:module-id: mNN

include::ROOT:partial$prereq-ws-start.adoc[]

== Exercise 1 — <name> [~10 min]

<What and why in one or two sentences.>

// Runnable blocks: role=execute makes them click-to-run in the Showroom terminal.
// Parameterized commands need subs="attributes"; real captured output must NOT.
[source,sh,role=execute,subs="attributes"]
----
oc get pods -n {user}-dev
----

// Expected output >5 lines goes in a collapsible block (5-= delimiter INSIDE tabs!).
.Expected output
[%collapsible]
====
[source,text]
----
<REAL output you captured while performing the lab>
----
====

✔ Verify:: <one concrete check the attendee performs before moving on.>

// Console labels you could not ground from CLI evidence:
// [CAPTURE-VERIFY] <exact label to confirm in a browser> — grounded CLI path beside it.

== Challenge (optional, no hand-holding)

<A stretch task using only what this module taught.>

ifdef::demo[]
== Demo arc

[demo-block]
[TIME 3m]
Say:: <talk track>
Show:: <screen>
Do:: <exact path/commands>
endif::demo[]
