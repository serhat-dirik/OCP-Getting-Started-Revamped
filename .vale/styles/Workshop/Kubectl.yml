# Attendees learn the platform, not a generic tool: prefer `oc` over `kubectl`
# (04-STYLE-GUIDE §5). Suggestion-level, so it surfaces without failing CI — the
# equivalence is stated once in M01, and a few deliberate `kubectl` mentions are fine.
extends: existence
message: "Prefer 'oc' over '%s' in attendee steps (state the equivalence once, in M01)."
level: suggestion
ignorecase: false
tokens:
  - kubectl
