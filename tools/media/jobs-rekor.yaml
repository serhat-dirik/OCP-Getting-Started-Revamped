# Rekor transparency-log entry (M08 trusted-supply-chain shot 04).
#
# PARKED — deliberately empty, do not "fix" by pointing it at the bare search page.
#
# The UI is public (no OAuth, so --no-auth is correct) and it loads fine. But the manifest asks
# for an ENTRY: "the keyless signature as a public, permanent transparency-log record (log
# index, integrated time, the issued identity)". Shooting the empty search form would produce a
# perfectly valid PNG of the wrong thing — the exact failure this harness exists to prevent, and
# worse in a document than no image at all.
#
# TO FINISH IT: run the M08 supply-chain lab so an image is genuinely signed, take the resulting
# digest, and search by it — the UI accepts the query in the URL (e.g. `/?hash=sha256:<digest>`).
# Then set wait_text to something only a RESULT page carries (a log-index value), never "Rekor".
#
# A wait_text trap found here and worth keeping: "Rekor" is in <title> but NOT in the body, and
# wait_text matches document.body.innerText. The body's own words are
# "Attribute / Email / Hash / Commit SHA / Entry UUID / Log Index / Search".
jobs: []
