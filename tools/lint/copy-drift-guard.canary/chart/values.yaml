---
# Values for the canary fixture chart. Their only job is to make the templates render with real
# substituted values, so the self-test proves the rendered copy — not the raw template text.
namespace: canary-ns
owner: ogsr
