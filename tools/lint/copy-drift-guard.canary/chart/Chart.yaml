---
# Canary chart for copy-drift-guard.py. It is never installed: it exists only so the guard's
# self-test exercises the real `helm template --show-only` -> parse -> select path, which is where
# a silent "rendered nothing, therefore clean" bug would otherwise hide.
apiVersion: v2
name: copy-drift-canary
description: Fixture chart for tools/lint/copy-drift-guard.py --self-test. Not for installation.
type: application
version: 0.1.0
