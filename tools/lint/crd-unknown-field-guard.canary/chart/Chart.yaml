# Canary chart for tools/lint/crd-unknown-field-guard.py. Rendered ONLY by that guard's --self-test,
# never by a cluster: it deliberately ships custom resources carrying fields their CRDs do not
# define, so that blinding any one of the guard's detectors changes an exit code.
apiVersion: v2
name: crd-unknown-field-canary
description: Planted unknown fields, and the legal fields beside them that must stay unflagged.
type: application
version: 0.1.0
