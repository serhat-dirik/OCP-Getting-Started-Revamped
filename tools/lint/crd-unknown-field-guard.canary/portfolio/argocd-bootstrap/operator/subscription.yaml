# PLANTED DEFECT: `spec.channell` is a typo for `channel`. The Subscription CRD's spec names seven
# fields and sets no x-kubernetes-preserve-unknown-fields, so the API server prunes it at admission.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: canary-bootstrap
  namespace: canary-operators
spec:
  channell: stable
  # The legal field beside it: if THIS fires, the walker is flagging everything.
  name: canary-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
