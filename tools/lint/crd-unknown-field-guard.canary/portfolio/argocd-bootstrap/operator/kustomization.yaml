# Canary twin of platform-portfolio/argocd-bootstrap/operator — the overlay PORTFOLIO_ALWAYS renders
# with no Application pointing at it. Drop that constant and the planted Subscription defect vanishes.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - subscription.yaml
