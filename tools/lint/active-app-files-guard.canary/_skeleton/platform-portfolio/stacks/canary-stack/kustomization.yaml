# Canary stack. Shaped like a real one INCLUDING the thing that matters: an app file that exists on
# disk but is commented out of resources:, exactly as stacks/observability does with loki-logging.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - apps/enabled.yaml
  # Deliberately NOT rendered — the file below exists, its Application never does.
  # - apps/disabled.yaml
