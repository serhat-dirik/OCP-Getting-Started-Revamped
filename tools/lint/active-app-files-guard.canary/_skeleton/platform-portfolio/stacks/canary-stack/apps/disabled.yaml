# On disk, commented out of the kustomization: the file a glob sees and the renderer never does.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pp-canary-disabled
spec:
  source:
    path: platform-portfolio/components/canary-disabled
