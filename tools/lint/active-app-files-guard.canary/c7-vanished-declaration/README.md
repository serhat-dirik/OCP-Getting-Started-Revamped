CANARY (no files of its own). Run under a ledger declaring `bootstrap/gone.sh`, which is not in the
tree. The entry suppresses nothing, and the next file to take that path would inherit a suppression
nobody chose. `.md` is not a scanned suffix, so this note cannot itself become a finding.
