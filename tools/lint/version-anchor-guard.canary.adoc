= version-anchor-guard canary

Fixture for `tools/lint/version-anchor-guard.py --self-test`. Every detector must fire here, and
the opt-out must suppress. If a run over this file comes back clean, the GUARD is broken.

NOT a real page — it is never built (it lives under tools/, outside the Antora content root).

== Detector 1: the attribute form

The Gateway API is GA on OpenShift {ocp_version} and is the strategic direction.

Native `ImagePolicy` shipped with OpenShift {ocp_image_policy_version}.

== Detector 2: the hardcoded form

This module was written against OpenShift 4.22 and assumes the unified console.

Console navigation matches v4.22.5 exactly.

== Detector 3: an opt-out with no reason

// version-anchor-ok:
Someone tried to silence OpenShift 4.22 without saying why.

== The opt-out must actually suppress

// version-anchor-ok: the catalog index tag genuinely tracks the cluster minor — naming a release IS the lesson
The index image tag tracks your cluster's OpenShift minor: EXEMPT-MUST-NOT-FIRE 4.22 on a 4.22
cluster, 4.21 on a 4.21 one. Both {ocp_version} references here must also stay silent.

== Comments never render, so they are never flagged

// Grounded 2026-07-29 on OCP 4.22.5 as user1, viewport 1600x1000 — this line must NOT be flagged.
// [CAPTURE-VERIFY] masthead Quick create (OCP {ocp_version}) — must NOT be flagged either.

This paragraph is clean prose with no release in it at all.

== Captured output is verbatim and must not be doctored

.Expected output
[source,texinfo]
----
Server Version: 4.22.5
Kubernetes Version: v1.35.0
catalog image: registry.redhat.io/redhat/redhat-operator-index:v4.22
----

....
oc version --short
Client Version: 4.22.5
....

A closing clean paragraph, so the file does not end mid-block.
