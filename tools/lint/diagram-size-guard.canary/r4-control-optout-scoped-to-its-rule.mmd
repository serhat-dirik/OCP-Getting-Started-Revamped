%% THE fixture for "per rule, not per file". Two unrelated defects, one marker.
%% `wide` has a four-line label (tall-label) AND the chain below is seven nodes (wide-chain). The
%% marker excuses tall-label only, so this file must still be RED for wide-chain. A per-FILE opt-out
%% passes every other fixture in this directory and fails exactly here: it would report silence, and
%% the seven-rank chain — a different defect, with a different fix, that no maintainer has looked
%% at — would ship excused by a note written about a label.
%% The tall label sits on its own line so that a marker which over-suppressed, under-suppressed, or
%% leaked across rules changes WHICH LINE fires, not just how many. `[stray]` catches the first,
%% `[wrong line]` the second, `[cross-talk]` the third.
%% EXPECT: wide-chain | blind EDGE_SPLIT_RE — with no link operator recognised there is no chain to
%% be too wide, so the surviving finding disappears and the file falls silent. That proves the
%% wide-chain reported here is the real detector rather than a leftover of the suppressed one.
%% diagram-size-ok: tall-label — excuses the label ONLY; the seven-node chain below stays red
flowchart LR
  wide["line one<br/>line two<br/>line three<br/>line four"]
%% MUST-FIRE
  wide --> c2["two"] --> c3["three"] --> c4["four"] --> c5["five"] --> c6["six"] --> c7["seven"]
