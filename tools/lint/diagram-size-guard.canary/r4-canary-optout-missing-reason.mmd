%% CANARY for the opt-out: a marker with the rule name and no reason.
%% The reason is what stops the convention becoming a rubber stamp. Without one, nobody reading this
%% file later can tell a diagram whose meaning genuinely fights its size from a diagram nobody got
%% round to fixing — and the second kind is what an unchecked opt-out quietly accumulates.
%% Note the marker below is ALSO stale (the file produces no tall-label), which is the point of the
%% precedence rule: one finding per marker, naming the mistake to fix FIRST, never a pile of two.
%% EXPECT: optout-no-reason | - — baseline only. OPTOUT_RE is owned by the sibling r4 fixtures; what
%% this one owns is the boundary between "a marker with a reason" and "a marker with none", so an
%% implementation that accepted a bare rule name would fail here rather than pass everywhere.
flowchart TB
%% MUST-FIRE
%% diagram-size-ok: tall-label
  a["one line"] --> b["also one line"]
