%% CANARY for rule 3 with every node shape mermaid offers, because a chain does not stop being wide
%% when its nodes are stadiums and cylinders. This is the fixture that proves node ids are still
%% recovered once the label brackets are masked away.
%% EXPECT: wide-chain | blind NODE_ID_RE — with no identifier recovered from a segment there are no
%% edges to chain, so the finding must go. Nothing else in this file can produce it.
flowchart LR
%% MUST-FIRE
  s1(["start"]) --> s2["box"] --> s3{{"hex"}} --> s4[("db")] --> s5(("dot")) --> s6[/"skew"/] --> s7["stop"]
