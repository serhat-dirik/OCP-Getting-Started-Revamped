%% CANARY for rule 1 where the crossing edge reaches two levels down. Membership has to be
%% TRANSITIVE: proj sits inside cluster, so an edge from the outside world to a node inside proj
%% crosses cluster's boundary as well, and cluster's direction is discarded too. Only cluster is
%% marked below because proj's own direction survives — nothing crosses proj's boundary except the
%% same edge, which does cross it, so both are findings and both are marked.
%% EXPECT: ignored-direction | blind SUBGRAPH_RE — with no subgraph recognised there is no boundary
%% to cross and no membership set to cross it, so the rule must go silent.
flowchart TB
%% MUST-FIRE
  subgraph cluster["OpenShift cluster"]
    direction LR
%% MUST-FIRE
    subgraph proj["Your project"]
      direction LR
      p1["one"] --> p2["two"]
    end
  end
  ext["outside"] --> p1
