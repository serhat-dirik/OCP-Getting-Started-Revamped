%% CONTROL for rule 1, and the witness for subgraph depth tracking.
%% The box declares direction and closes; the two nodes below it are outside, and the edge joining
%% them touches the box's membership set at neither end. Nothing is discarded, so nothing is
%% reported — but that verdict depends entirely on the guard knowing where the box ENDED.
%% EXPECT: none | blind END_RE — with no end recognised the box never closes, "later" is absorbed
%% into it while "top" was already placed outside, and the edge top --> later becomes a crossing of
%% a boundary that should not have been there. This file must start firing.
flowchart TB
  top["declared before the box"]
  subgraph A["left box"]
    direction LR
    a1["one"] --> a2["two"]
  end
  later["declared after the box"]
  top --> later
