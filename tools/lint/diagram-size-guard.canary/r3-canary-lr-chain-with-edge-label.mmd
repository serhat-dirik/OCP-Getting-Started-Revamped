%% CANARY for rule 3 where one link carries a label containing link-like text. The corpus really
%% does write things like oc set volume --overwrite inside an edge label, and those characters are
%% mermaid link syntax. Masking edge labels before the chain is walked is what keeps the run intact.
%% EXPECT: wide-chain | blind EDGE_LABEL_RE — unmasked, the label text splits the first link in
%% two, the run drops to six nodes and the finding disappears. Its survival proves the masking.
flowchart LR
%% MUST-FIRE
  e1["one"] -->|"oc set volume --overwrite"| e2["two"] --> e3["three"] --> e4["four"] --> e5["five"] --> e6["six"] --> e7["seven"]
