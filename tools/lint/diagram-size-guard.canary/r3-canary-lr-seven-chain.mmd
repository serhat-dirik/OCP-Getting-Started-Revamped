%% CANARY for rule 3, the wide shape. Seven nodes in one top-level LR run, nothing wrapping them.
%% Mermaid lays this out at roughly 2000px and head-styles.hbs then scales it DOWN into the 900px
%% column, which is how a diagram ends up with 0.44x text and a 66px height.
%% EXPECT: wide-chain | blind EDGE_SPLIT_RE — the rule counts nodes along LINKS, so with no link
%% operator recognised there is no chain to be too long, and the finding must disappear.
graph LR
%% MUST-FIRE
  c1["one"] --> c2["two"] --> c3["three"] --> c4["four"] --> c5["five"] --> c6["six"] --> c7["seven"]
