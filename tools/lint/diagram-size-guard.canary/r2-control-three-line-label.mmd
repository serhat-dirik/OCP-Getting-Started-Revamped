%% CONTROL for rule 2, sitting exactly on the boundary: three lines is fine, four is not.
%% A naive implementation — "the file contains br" — fires here and would flag 60 correct diagrams.
%% EXPECT: none | flood LABEL_BREAK_RE — flood the break pattern and every label looks infinitely
%% tall, so this file must start firing. Its silence is therefore the threshold doing work.
flowchart LR
  a["line one<br/>line two<br/>line three"] --> b["line one<br/>line two"]
