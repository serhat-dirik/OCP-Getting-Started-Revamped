%% CONTROL for rule 1. The same boundary-crossing edge as the canary, but no direction line
%% anywhere: nothing is being discarded because nothing was declared, and the source is not
%% claiming a layout it fails to get. Measured on the real tree —
%% storage-stateful/02-sts-vs-deployment.mmd declares no direction and renders perfectly well.
%% A naive implementation — "any subgraph with an edge crossing its boundary" — fires here and
%% would redden roughly twenty correct files on the day it landed.
%% EXPECT: none | - — baseline only. Every pattern is owned by another fixture; what this one owns
%% is the rule that a missing declaration is not a discarded one.
flowchart TB
  subgraph A["left box"]
    a1["one"] --> a2["two"]
  end
  subgraph B["right box"]
    b1["three"] --> b2["four"]
  end
  a2 --> b1
