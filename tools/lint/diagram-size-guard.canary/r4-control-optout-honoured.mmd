%% CONTROL for the opt-out: a real four-line label, deliberately excused.
%% The node below is a genuine tall-label offender and this file must still scan SILENT, because a
%% well-formed marker names that rule and gives a reason. If the marker did not suppress, every
%% deliberate exception in the corpus would be a permanent red line and the guard would be switched
%% off — which is the outcome every docstring in this directory is written against.
%% EXPECT: none | blind OPTOUT_RE — blind the marker pattern and the label underneath it becomes the
%% offender it really is. Silence here is caused by exactly one thing, and this proves which.
%% diagram-size-ok: tall-label — the marker must reach the report, so the reason is asserted verbatim
flowchart TB
  excused["line one<br/>line two<br/>line three<br/>line four"] --> ok["two<br/>lines"]
