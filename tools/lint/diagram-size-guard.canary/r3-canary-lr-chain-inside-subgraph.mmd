%% CANARY for rule 3 in the shape an earlier draft got wrong. The seven-node run is declared INSIDE
%% a subgraph, and an earlier version of this rule treated "wrapped" as "narrow" and scored it
%% clean. Wrapping does not remove a dagre rank — it draws a box around some of them — and the box's
%% own direction TB is discarded anyway the moment the edge into it crosses the boundary.
%% app-security-testing/01-five-pillars.mmd is the real file that scored clean at eight ranks.
%% EXPECT: wide-chain | - — baseline only. Every pattern is owned by another rule 3 fixture; what
%% this one owns is the rule that chain length is counted wherever the links are written.
graph LR
  subgraph SL["shift left"]
%% MUST-FIRE
    k1["one"] --> k2["two"] --> k3["three"] --> k4["four"] --> k5["five"] --> k6["six"] --> k7["seven"]
  end
