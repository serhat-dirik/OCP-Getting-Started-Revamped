%% CONTROL for rule 3, sitting exactly on the boundary: six nodes across a 900px column is legible,
%% seven is not. A naive implementation that counts node ids rather than chain length fires here.
%% EXPECT: none | - — baseline only. The canaries next door own the patterns; this file owns the
%% threshold, and an off-by-one in MAX_CHAIN_NODES is a failing self-test rather than a quiet
%% widening of the rule.
graph LR
  d1["one"] --> d2["two"] --> d3["three"] --> d4["four"] --> d5["five"] --> d6["six"]
