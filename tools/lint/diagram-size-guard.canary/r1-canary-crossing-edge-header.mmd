%% CANARY for rule 1 in an LR parent, which is where the discarded direction hurts most: the boxes
%% were meant to stack as TB columns and instead everything flattens into one very wide row.
%% EXPECT: ignored-direction | blind HEADER_RE — no header means no diagram, and every rule here
%% stops applying to a file that is not a flowchart at all. That early return is what this fixture
%% proves is wired.
flowchart LR
%% MUST-FIRE
  subgraph L["left column"]
    direction TB
    l1["one"] --> l2["two"]
  end
  m["middle"] --> l1
