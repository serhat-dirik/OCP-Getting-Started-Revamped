%% CONTROL for the opt-out's SYNTAX boundary: prose that talks about the marker is not a marker.
%% Both comment lines below name the convention, and neither may be read as invoking it. A guard
%% that matched the keyword anywhere in a comment would let a sentence ABOUT an exception become an
%% exception — and this corpus writes long `%%` headers explaining exactly these rules, so that is
%% not a hypothetical shape. A marker must open its own line, immediately after the `%%`.
%% EXPECT: none | flood OPTOUT_RE — flood the marker pattern and every line in this file reads as a
%% marker naming no rule, so the silence breaks into optout-unknown-rule findings. Without that,
%% this file's quiet would be indistinguishable from a guard that had stopped looking for markers.
%% This diagram needs no diagram-size-ok marker: it has no finding to excuse.
%% See the module docstring for what %% diagram-size-ok: would mean if it opened a line.
flowchart TB
  a["one line"] --> b["also one line"]
