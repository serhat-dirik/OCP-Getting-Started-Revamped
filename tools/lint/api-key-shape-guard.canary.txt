# Canary fixture for tools/lint/api-key-shape-guard.py — read by --self-test, by nothing else.
#
# EVERY STRING BELOW IS SYNTHETIC. The catchable ones were CONSTRUCTED to be shape-equivalent to
# published key formats and to the measured properties of this project's own key (length, separator
# set, chunk lengths — properties only; the key itself was never transcribed anywhere). None of them
# authenticates against anything.
#
# The rules the self-test reads (the marker itself is spelled out in the guard as
# MUST_NOT_CATCH_MARKER; it is not repeated in this paragraph, or this paragraph would count as one
# of the fixtures):
#   * a line carrying the benign marker MUST NOT be flagged — these are the look-alikes that made
#     the naive widening `sk-[A-Za-z0-9_-]{16,}` unusable, kept verbatim from the real tree.
#   * any other non-blank, non-comment line MUST be flagged. That count is asserted against
#     MUST_CATCH in the guard, so deleting a shape here cannot quietly shrink what is proven.
#
# The one real-tree string not represented here is the credential-redaction guard's own fixture
# (tools/lint/credential-redaction-guard.canary.adoc). It IS key-shaped and is meant to be — it is
# excluded by PATH, not by the pattern, and the self-test asserts that exclusion directly rather than
# bending the discriminator around a fixture.

# --- MUST CATCH: shapes a real key has ---------------------------------------------------------

# The shape the old `sk-[A-Za-z0-9]{20}` pattern missed: one separator inside the first 20 chars.
sk-x_A7bQ2mZ4kR9tW1nX6vC8
# 48 unbroken alphanumerics — the classic long-form shape.
sk-Xy7Qm2Rt9Vb4Nk6Hs1Jd8Fg3Lp5Zc0Ww2Ee4Rr6Tt8Yy0Uu1
# A vendor-prefixed base64url body, where `-` and `_` chop the run into pieces.
sk-ant-api03-9fK2mQx7Rt4Vb-Nk6Hs1Jd8Fg3Lp5Zc0_Ww2Ee4Rr6Tt8Yy0Uu1AA
# 32 lowercase hex — no uppercase at all, so an "entropy = has uppercase" rule would miss it.
sk-9f3b7c1d2e4a6b8c0d1e3f5a7b9c1d3e
# Project-prefixed with an underscore separator.
sk-proj-Ab3Cd5Ef7Gh9Ij1Kl_Mn3Op5Qr7St9Uv1Wx
# Uppercase and digits only.
sk-ABCD1234EFGH5678IJKL
# Lowercase and digits only.
sk-abc123def456ghi789jkl012
# In the position a key actually gets committed in: the value half of an env assignment.
GENAI_API_KEY=sk-Zq8Wc2Xe4Rv6Tb1Yn3Um5Io7Pa9Sd

# --- MUST NOT CATCH: the look-alikes already in the tree ----------------------------------------

private static final String FAKE_KEY = "sk-SYNTHETIC-not-a-real-key-0123456789";    # BENIGN
parasol-task-image-size-report                                                      # BENIGN
parasol-task-roxctl-deployment-check                                                # BENIGN
sk-image-size-report                                                                # BENIGN
sk-configuration-management-service                                                 # BENIGN
disk-encryption-configuration-2024                                                  # BENIGN
# Pins MIN_OPAQUE_CHUNK from BELOW: `v11` is a short chunk with both a letter and a digit, so a
# loosened threshold starts flagging ordinary release slugs. Without this line the self-test passes
# with the threshold set as low as 3 — the layers would be proven but their calibration would not.
sk-parasol-web-v11-rollout-2026                                                     # BENIGN
