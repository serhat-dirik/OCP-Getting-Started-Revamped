# Deliberately broken YAML. Proves that a descriptor which does not PARSE is reported as
# "the guard could not read what it compares" (rc 2) rather than as "no soft defaults", which
# would read as "nothing to check" and pass.
#
# Named .yml.fixture, not .yml, so no repo-wide YAML linter ever tries to parse it and fail.
asciidoc:
  attributes:
    ocp_console_url: "https://console.apps.example.com@
    cluster_domain: [unclosed
   bad_indent: :
