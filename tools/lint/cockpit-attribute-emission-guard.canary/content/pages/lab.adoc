= Canary lab page

Canary content for cockpit-attribute-emission-guard.py --self-test. Not workshop content; it exists
only so the reference scanner has pages with a known answer.

The two attributes at the centre of instance 2, referenced the way a real lab page references them:

Open the developer portal at {rhdh_url} and sign in as a guest.

Then open your own Argo CD at {student_argo_url} and watch the application sync.

A dual-path step, so the same attribute appears more than once:

[tabs]
======
Console::
+
--
Browse to {student_argo_url} and select the application.
--

CLI::
+
--
[source,bash]
----
oc get applications -n {user}-gitops
----
--
======

Attributes that are NOT placeholder-shaped appear here too, so the classifier is doing the
excluding rather than the reference scanner: you are {user}, and your cluster is {cluster_domain}.

A real host that merely contains "example" as a DNS label — if the reserved-domain rule
over-matches, this attribute joins the required set and a correct default becomes a finding:
{real_host_url}.

// COMMENT-ONLY REFERENCE, and the only mention of {maas_endpoint} anywhere in this fixture.
// References are counted RAW on purpose: stripping comments is the failing-OPEN direction and
// would require this guard to know that `//` is a comment at top level but literal text inside a
// listing block. Getting that wrong drops a real reference and ships a dead link. If a future
// edit starts stripping comments, the self-test's assertion on this line fails.
