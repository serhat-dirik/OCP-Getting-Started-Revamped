= Canary concept page

A SECOND page, so the reference scanner's rglob has something to descend into — a scanner that
reads one file and stops looks identical to one that works, right up until the attribute it missed
was the one that mattered.

Instance 1's two attributes, referenced here rather than on the lab page: the SAST findings live in
SonarQube at {sonarqube_url}, and the image scan results in RHACS at {acs_console_url}.

Instance 3 added a third to the demos cockpit: the migration console at {mta_url}.

The sentinel family, which is placeholder-shaped for the same reason a reserved domain is — the
value names where the real one comes from, and renders successfully while being wrong. The model
this cluster serves is {maas_model}, on a cluster running {cluster_ocp_version}.

The console itself: {ocp_console_url}.

// `never_mentioned_url` deliberately appears ONLY in this comment and ONLY as prose, never in
// braces, so the scanner has an attribute it must NOT report as referenced.
