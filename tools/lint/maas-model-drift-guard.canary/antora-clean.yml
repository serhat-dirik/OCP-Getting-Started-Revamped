# canary fixture — mimics ONLY the shape of content/antora.yml's asciidoc.attributes.maas_model
# default that maas-model-drift-guard.py's extractor depends on. Agrees with versions.yaml's
# canary `maas.model` (llama-scout-17b) — this pair must come back CLEAN.
asciidoc:
  attributes:
    maas_model: "llama-scout-17b@"
