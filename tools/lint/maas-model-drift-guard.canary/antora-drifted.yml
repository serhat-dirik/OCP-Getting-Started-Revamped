# canary fixture — same shape as antora-clean.yml, but the value disagrees with versions.yaml's
# canary `maas.model` (llama-scout-17b) on purpose. This pair must come back DRIFTED.
asciidoc:
  attributes:
    maas_model: "qwen3-14b@"
