# canary fixture — mimics ONLY the shape of the real versions.yaml `maas:` block that
# maas-model-drift-guard.py's extractor depends on. Not the real file; do not "fix" this to match
# production values.

# A preceding top-level entry, to prove the extractor does not just grab the first `model:` line
# in the document.
kubernetes_mcp_server:
  repo: github.com/containers/kubernetes-mcp-server
  version: "0.0.65"

maas:
  endpoint_attribute: "{maas_endpoint}"   # content references the endpoint ONLY via this attribute
  model: llama-scout-17b                 # verified live (task #67 QA gate)
  source_secret: openshift-lightspeed/credentials
  source_key: apitoken

# A following top-level entry with its OWN `model:`-shaped key, to prove the extractor stops at
# the end of the `maas:` block instead of matching this one too.
another_component:
  model: should-never-be-picked-up
