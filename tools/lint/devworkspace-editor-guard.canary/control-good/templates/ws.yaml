# CONTROL — the shape the dashboard itself produces. Must stay silent.
apiVersion: workspace.devfile.io/v1alpha2
kind: DevWorkspaceTemplate
metadata:
  name: che-code-canary
---
apiVersion: workspace.devfile.io/v1alpha2
kind: DevWorkspace
metadata:
  name: canary-ws
spec:
  started: true
  contributions:
    - name: editor
      kubernetes:
        name: che-code-canary
