# CANARY — an `editor` contribution carrying neither `kubernetes.name` nor `uri`.
apiVersion: workspace.devfile.io/v1alpha2
kind: DevWorkspace
metadata:
  name: canary-ws
spec:
  started: true
  contributions:
    - name: editor
