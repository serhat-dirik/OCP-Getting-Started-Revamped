# CANARY — a chart that cannot render. `render()` returning an error must be a FINDING, never a
# silent skip: a chart nobody can render is a chart nobody is checking.
{{- fail "devworkspace-editor-guard fixture: this chart is meant to fail to render" }}
