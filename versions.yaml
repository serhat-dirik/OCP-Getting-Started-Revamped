# SINGLE SOURCE OF TRUTH for product versions used by this workshop.
# Rule (01-ARCHITECTURE §6): content/slides reference versions ONLY via attributes
# generated from this file. Any session touching a module must confirm `verified`
# is <60 days old for that product, or re-verify against docs.redhat.com first.
#
# STATUS: initial stub — first full verification pass in progress (Phase 0, run 1).
verified: null  # set per-product below; top-level date = oldest product verify date

ocp:
  channel: stable-4.21
  version: "4.21.22"          # actual build cluster (cluster-qvkd5); content targets 4.20+
  verified: 2026-07-08
  notes: Build cluster runs 4.21.22 (verified live). Docs assumed 4.20 EUS at planning time.
