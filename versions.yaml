# SINGLE SOURCE OF TRUTH for product versions used by this workshop.
# Rule (01-ARCHITECTURE §6): content/slides reference versions ONLY via attributes
# generated from this file. Any session touching a module must confirm `verified`
# is <60 days old for that product, or re-verify against docs.redhat.com first.
#
# Primary source: live OLM packagemanifests on build cluster ocp-ws-revamped (OCP 4.21.22),
# queried 2026-07-08. Cross-checks: docs.redhat.com / developers.redhat.com / access.redhat.com.
verified: 2026-07-08

ocp:
  channel: stable-4.21
  version: "4.21.22"
  eus_line: "4.20"          # 4.20 = current EUS (even-numbered, 24-mo); 4.21 = standard support (odd)
  verified: 2026-07-08
  source: access.redhat.com/support/policy/updates/openshift
  notes: Build cluster runs 4.21.22 (live). Content targets 4.20+ (EUS). 4.21 is NOT EUS; 4.22 is next EUS.
  entitlement: OCP

# ---------- PRIORITY (platform + M01) ----------
gitops:
  package: openshift-gitops-operator
  channel: latest           # == gitops-1.21
  csv: openshift-gitops-operator.v1.21.1
  version: "1.21.1"
  argocd: "3.4"             # up from 3.3 in GitOps 1.20
  verified: 2026-07-08
  source: developers.redhat.com/blog/2026/06/26/whats-new-openshift-gitops-121
  notes: >
    Argo Rollouts (RolloutManager + Rollout CRDs present on cluster) GA since GitOps 1.13.
    Argo CD Image Updater and Argo CD CLI now GA in 1.21. GitOps Console Plugin = Tech Preview in 1.21.
  entitlement: OCP

argocd:
  # The Argo CD version BUNDLED by the GitOps operator above (mirrors gitops.argocd; kept as
  # its own top-level entry so gen-attributes emits {argocd_version} for content prose).
  version: "3.4"
  verified: 2026-07-10
  source: live cluster — openshift-gitops + student-gitops instances on GitOps 1.21.1
  entitlement: OCP

pipelines:
  package: openshift-pipelines-operator-rh
  channel: latest           # == pipelines-1.22
  csv: openshift-pipelines-operator-rh.v1.22.4
  version: "1.22.4"
  verified: 2026-07-08
  notes: Pipelines-as-Code bundled + GA (since Pipelines 1.9). Use cluster resolvers, not Tekton Hub-as-product (banned).
  entitlement: OCP

devspaces:
  package: devspaces
  channel: stable
  csv: devspacesoperator.v3.29.0
  version: "3.29.0"
  verified: 2026-07-09
  che_version: "3.29.0"
  udi_image: registry.redhat.io/devspaces/udi-rhel9:3.29   # digest sha256:84c9b0e6ab68... (CSV relatedImages, live cluster)
  editor_image: registry.redhat.io/devspaces/code-rhel9    # che-code = default browser editor (VS Code)
  notes: >
    Dev Spaces (was CodeReady Workspaces — banned name). devfile 2.2.x; DevWorkspace engine; default editor che-code.
    R4 2026-07-09: cluster auto-upgraded 3.28.2 -> 3.29.0 via 'stable' channel
    (live CSV devspacesoperator.v3.29.0 Succeeded, cheVersion 3.29.0 on ocp-ws-revamped).
    docs.redhat.com doc-set currently indexes up to 3.26 (checked 2026-07-09) -- cite nearest-published doc-set until 3.29 docs land.
  entitlement: OCP

web_terminal:
  package: web-terminal
  channel: fast
  csv: web-terminal.v1.16.1
  version: "1.16.1"
  verified: 2026-07-08
  notes: Ships with OCP; backed by DevWorkspace operator.
  entitlement: OCP

cert_manager:
  package: openshift-cert-manager-operator   # NOT community 'cert-manager'
  channel: stable-v1
  csv: cert-manager-operator.v1.20.0
  version: "1.20.0"
  verified: 2026-07-08
  notes: cert-manager Operator for Red Hat OpenShift. Operator version tracks upstream 1:1, so v1.20.0 ≈ upstream cert-manager 1.20.x.
  entitlement: OCP

lightspeed:
  package: lightspeed-operator
  channel: stable            # alpha channel also = v1.1.1
  csv: lightspeed-operator.v1.1.1
  version: "1.1.1"
  verified: 2026-07-08
  source: docs.redhat.com/en/documentation/red_hat_openshift_lightspeed
  notes: >
    OpenShift Lightspeed (console assistant) GA 2025-05-20; supported OCP 4.16+. OLSConfig ols.openshift.io/v1alpha1
    (CR name must be 'cluster'); provider type enum includes rhoai_vllm with url field -> MaaS-wireable
    (secret key 'apitoken'). Entitlement CONFIRMED by Serhat 2026-07-09: OpenShift Lightspeed = included
    with OCP; Developer Lightspeed (RHDH/MTA) = Advanced Developer Suite.
  entitlement: OCP

gitea:
  package: null              # NOT in OperatorHub — RHPDS/GPTE operator
  install: "kustomize remote base github.com/rhpds/gitea-operator/OLMDeploy?ref=<pinned SHA>"
  pinned_sha: 77cdc2b884c160663f7ef0b9040c35898d3ebcce
  cr_apiVersion: pfe.rhpds.com/v1
  cr_kind: Gitea
  verified: 2026-07-08
  source: github.com/rhpds/gitea-operator
  notes: Not OperatorHub-native; wrapped as SHA-pinned remote kustomize base in platform-portfolio/components/gitea.
  entitlement: OCP           # tooling, not a Red Hat product SKU

# ---------- SECONDARY (verified one-row entries) ----------
service_mesh:
  package: servicemeshoperator3   # OSSM3. v2 'servicemeshoperator' (SMCP/SMMR) is BANNED
  channel: stable
  csv: servicemeshoperator3.v3.3.5
  version: "3.3.5"
  verified: 2026-07-08
  notes: Istio/Sail CRs; ambient GA since OSSM 3.2 (see ADR-0003). 'candidates' channel holds stale v3.0.0-tp.2 — avoid.
  entitlement: OCP

serverless:
  package: serverless-operator
  channel: stable
  csv: serverless-operator.v1.37.1
  version: "1.37.1"
  verified: 2026-07-08
  entitlement: OCP

rhdh:
  package: rhdh
  channel: fast              # == fast-1.10; no 'stable' channel — 'fast' is the release channel
  csv: rhdh-operator.v1.10.2
  version: "1.10.2"
  verified: 2026-07-08
  entitlement: ADS

acs:
  package: rhacs-operator
  channel: stable            # NOT 'latest' (holds stale v3.74.9)
  csv: rhacs-operator.v4.11.1
  version: "4.11.1"
  verified: 2026-07-11
  notes: >
    Advanced Cluster Security. Installed live 2026-07-11 (platform-portfolio trust stack): operator
    CSV Succeeded, Central + SecuredCluster (workshop-cluster) Healthy. 'latest' channel still holds
    stale v3.74.9 — use 'stable'. Central/SecuredCluster = platform.stackrox.io/v1alpha1;
    SecurityPolicy = config.stackrox.io/v1alpha1. Cluster-local registry scanning needs delegated
    scanning enabled (Central API /v1/delegatedregistryconfig enabledFor=ALL) — the trust stack does it.
  entitlement: ADS

rhtas:
  package: rhtas-operator
  channel: stable            # == stable-v1.4; serves v1.4.2 as of 2026-07-11 (was v1.4.1 on 2026-07-08)
  csv: rhtas-operator.v1.4.2
  version: "1.4.2"
  verified: 2026-07-11
  notes: >
    Trusted Artifact Signer. Channel-pinned (stable) so the 1.4.1->1.4.2 bump is automatic. Securesign
    CR (rhtas.redhat.com/v1alpha1) TSA signer.certificateChain requires ALL of rootCA+intermediateCA+
    leafCA (operator rejects rootCA-only). Fulcio OIDCIssuers Type is a free string (use 'kubernetes'
    + https://kubernetes.default.svc for keyless-from-Tekton). DEMO-ONLY for M08 (keyless); the M08 LAB
    signs key-based via Tekton Chains, which needs only trust-signing (cosign key), NOT RHTAS.
  entitlement: ADS

rhtpa:
  package: rhtpa-operator
  channel: stable-v1.1
  csv: rhtpa-operator.v1.1.6
  version: "1.1.6"
  verified: 2026-07-11
  notes: >
    Trusted Profile Analyzer. OwnNamespace/SingleNamespace install (NOT AllNamespaces). Optional
    trust-demo component, instructor-only, PROVISIONAL (build note + PM 2026-07-09) — operator packaged
    in platform-portfolio/components/rhtpa but NOT installed live (cut-candidate; ~12-15 pods + needs
    OIDC/object-store/Postgres contracts).
  entitlement: ADS

mta:
  package: mta-operator
  channel: stable-v8.1
  csv: mta-operator.v8.1.2
  version: "8.1.2"
  verified: 2026-07-08
  notes: Migration Toolkit for Applications 8.1.
  entitlement: OCP

rhbk:
  package: rhbk-operator      # Red Hat build of Keycloak. 'RH-SSO' name BANNED
  channel: stable-v26.6
  csv: rhbk-operator.v26.6.4-opr.1
  version: "26.6"
  verified: 2026-07-09
  source: packagemanifest rhbk-operator (Red Hat Operators), live catalog ocp-ws-revamped
  notes: >
    Catalog displayName is generic "Keycloak Operator"; product = Red Hat build of Keycloak 26.6
    (stable-v26.6 = catalog head, what a NEW portfolio install pulls). CRDs keycloaks + keycloakrealmimports
    at k8s.keycloak.org/v2alpha1 (live). Standard token exchange (feature token-exchange-standard, RFC 8693) is
    GA + enabled by default since Keycloak 26.2 -> M29's advanced exercise is lab-viable, not preview.
    M29 NOTE: the live cluster LOGIN IdP (ns keycloak, Keycloak CR 'keycloak', realm import 'sso', client idp-4-ocp)
    runs the OLDER installed CSV rhbk-operator.v26.4.13-opr.1 (stable-v26.4) -- treat READ-ONLY; M29 stands up its own
    workshop instance rather than reusing it.
  entitlement: OCP

virtualization:
  package: kubevirt-hyperconverged
  channel: stable
  csv: kubevirt-hyperconverged-operator.v4.21.10
  version: "4.21.10"
  verified: 2026-07-08
  notes: OpenShift Virtualization; version tracks OCP minor. Bare-metal nodes required (M19).
  entitlement: OCP

oadp:
  package: redhat-oadp-operator
  channel: stable
  csv: oadp-operator.v1.5.7
  version: "1.5.7"
  verified: 2026-07-08
  entitlement: OCP

rhsi:
  package: skupper-operator
  channel: stable-2
  csv: skupper-operator.v2.2.1-rh-1
  version: "2.2.1"
  verified: 2026-07-08
  notes: Red Hat Service Interconnect (Skupper v2). stable-1 line (v1.9.x) is legacy — do not use.
  entitlement: ADD-ON

streams_kafka:
  package: amq-streams        # OLM package name unchanged; product RENAMED "Streams for Apache Kafka" (old name banned in prose)
  channel: stable
  csv: amqstreams.v3.2.0-26
  version: "3.2.0"
  verified: 2026-07-08
  notes: Catalog displayName still shows the legacy name; content must use "Streams for Apache Kafka".
  entitlement: ADD-ON

kueue:
  package: kueue-operator
  channel: stable-v1.3
  csv: kueue-operator.v1.3.1
  version: "1.3.1"
  verified: 2026-07-08
  notes: Red Hat build of Kueue (GA packaging exists). Included-with-OCP entitlement to confirm (D16 dagger note).
  entitlement: OCP           # provisional — confirm

keda:
  package: openshift-custom-metrics-autoscaler-operator
  channel: stable
  csv: custom-metrics-autoscaler.v2.19.0-1
  version: "2.19.0"
  verified: 2026-07-08
  notes: Custom Metrics Autoscaler (KEDA-based).
  entitlement: OCP

rhoai:
  package: rhods-operator
  channel: stable-3.x         # WARNING: bare 'stable' still points at old 2.x (2.25.8)
  csv: rhods-operator.3.4.2
  version: "3.4.2"
  verified: 2026-07-08
  notes: Red Hat OpenShift AI 3.4. Llama Stack = Tech Preview (see ADR-0004). High churn — re-verify at M22 build.
  entitlement: ADD-ON

serverless_logic:
  package: logic-operator     # Red Hat pkg; NOT community 'sonataflow-operator'
  channel: stable
  csv: logic-operator.v1.38.0
  version: "1.38.0"
  verified: 2026-07-08
  notes: OpenShift Serverless Logic. GA/TP status still unverified — M24 research gate re-checks per spec.
  entitlement: OCP

quarkus:
  version: "3.33.2.1"
  stream: "3.33 LTS"
  jdk_baseline: "21"
  verified: 2026-07-09
  source: quarkus.io/releases
  notes: >
    Current LTS (maintained until 2027-03; 3.20 LTS retired 2026-03-28). Used by all Parasol services.
    JDK baseline is 21+ everywhere (Serhat directive 2026-07-09): Containerfiles use ubi9/openjdk-21,
    poms set maven.compiler.release 21, S2I uses the workshop 'java-21' builder ImageStream
    (the stock 'java' stream tops out at openjdk-17-ubi8 — never use it in content).
  entitlement: OCP

external_secrets:
  package: openshift-external-secrets-operator   # Red Hat operator; NOT community 'external-secrets-operator' (alpha)
  channel: stable-v1
  csv: openshift-external-secrets-operator.v1.1.0
  version: "1.1.0"
  verified: 2026-07-08
  notes: External Secrets Operator for Red Hat OpenShift (supported). Community pkg of same purpose also in catalog — do not use.
  entitlement: OCP

# ---------- OBSERVABILITY (M11) — verified 2026-07-09 vs live OLM catalog on ocp-ws-revamped ----------
cluster_observability_operator:
  package: cluster-observability-operator
  channel: stable
  csv: cluster-observability-operator.v1.5.1
  version: "1.5.1"
  verified: 2026-07-09
  source: packagemanifest cluster-observability-operator (Red Hat Operators); docs.redhat.com/.../red_hat_openshift_cluster_observability_operator/1-latest
  notes: >
    Cluster Observability Operator (COO). AllNamespaces install. Owns UIPlugin (uiplugins.observability.openshift.io/v1alpha1)
    — console plugins Dashboards(Perses) / DistributedTracing (Observe->Traces) / Logging (Observe->Logs) / Monitoring /
    TroubleshootingPanel — plus MonitoringStack, Perses/PersesDashboard, ThanosQuerier (monitoring.rhobs/v1alpha1).
    Current dashboards/tracing/logging console path in 4.21. NOT installed on build cluster.
  entitlement: OCP

tempo:
  package: tempo-product          # Red Hat "Tempo Operator". Community 'tempo-operator' also in catalog — do not use.
  channel: stable
  csv: tempo-operator.v0.21.0-2
  version: "0.21.0-2"
  verified: 2026-07-09
  source: packagemanifest tempo-product (Red Hat Operators), live catalog
  notes: >
    Distributed-tracing store. Owns TempoMonolithic + TempoStack (tempo.grafana.com/v1alpha1). AllNamespaces.
    TempoMonolithic (single pod, memory/PV backend) = workshop-light; TempoStack needs S3 object storage
    (ODF NooBaa OBC storageclass openshift-storage.noobaa.io present). NOT installed.
  entitlement: OCP

opentelemetry:
  package: opentelemetry-product  # Red Hat build of OpenTelemetry. Community 'opentelemetry-operator' also in catalog — do not use.
  channel: stable
  csv: opentelemetry-operator.v0.152.0-1
  version: "0.152.0-1"
  verified: 2026-07-09
  source: packagemanifest opentelemetry-product (Red Hat Operators), live catalog
  notes: >
    Owns OpenTelemetryCollector (opentelemetry.io/v1beta1) + Instrumentation (v1alpha1). AllNamespaces. Collector receives
    OTLP (4317 gRPC) from parasol-claims and exports to Tempo. Parasol apps already ship quarkus-opentelemetry
    (exporter off via quarkus.otel.sdk.disabled=true until M11). NOT installed.
  entitlement: OCP

loki:
  package: loki-operator          # Red Hat "Loki Operator" (log store). Community 'loki-operator' (Grafana SIG, alpha) also in catalog — do not use.
  channel: stable-6.5
  csv: loki-operator.v6.5.1
  version: "6.5.1"
  verified: 2026-07-09
  source: packagemanifest loki-operator (Red Hat Operators), live catalog
  notes: >
    Owns LokiStack (loki.grafana.com/v1). Needs S3 object storage — ODF NooBaa OBC present. Smallest footprint size
    1x.demo (alm-example ships 1x.small — verify enum at build). Pairs with cluster-logging collector + COO logging UIPlugin.
    Heaviest M11 add (Vector DaemonSet 1 pod/node + ~6 Loki pods) — keep optional/instructor for lean events. NOT installed.
  entitlement: OCP

cluster_logging:
  package: cluster-logging         # Red Hat OpenShift Logging (log collection: ClusterLogForwarder / Vector)
  channel: stable-6.5
  csv: cluster-logging.v6.5.1
  version: "6.5.1"
  verified: 2026-07-09
  source: packagemanifest cluster-logging (Red Hat Operators), live catalog
  notes: >
    Logging 6.x. ClusterLogForwarder collects pod logs (Vector) -> LokiStack. Console Logs UI is COO logging UIPlugin.
    NOT installed.
  entitlement: OCP

vertical_pod_autoscaler:
  package: vertical-pod-autoscaler
  channel: stable
  csv: vertical-pod-autoscaler.v4.21.0-202606301919
  version: "4.21.0"
  verified: 2026-07-09
  source: packagemanifest vertical-pod-autoscaler (Red Hat Operators), live catalog
  notes: >
    VPA operator (version tracks OCP minor). M11 CONCEPT-ONLY in the scaling decision tree — VPA and HPA must not both
    manage CPU on the same target. NOT installed.
  entitlement: OCP

ocp_image_policy:
  api: config.openshift.io/v1
  status: GA
  version: "4.21"
  verified: 2026-07-09
  source: >
    live CRDs imagepolicies/clusterimagepolicies.config.openshift.io + oc explain imagepolicy.spec;
    docs.redhat.com OCP 4.20 config_apis clusterimagepolicy-config-openshift-io-v1;
    developers.redhat.com whats-new-developers-openshift-4-21
  notes: >
    Native sigstore image-signature admission, core OCP. ImagePolicy is NAMESPACED — carries the per-user
    "block unsigned images" beat in M07 as [OCP]; ClusterImagePolicy is the cluster-wide variant.
    GA since 4.20; 4.21 adds core-payload verification. policyType: PublicKey | FulcioCAWithRekor | PKI.
  entitlement: OCP
