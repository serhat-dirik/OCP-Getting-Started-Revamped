apiVersion: v2
name: m21-entry
description: "M21 Resilience, Multi-Cluster & DR — a genuinely resilient Parasol claims stack in the workshop-owned {user}-resilience namespace (parasol-claims Deployment at 3 replicas + PodDisruptionBudget + soft topologySpreadConstraints across hostname/zone + HorizontalPodAutoscaler) fronting a PERSISTENT, deterministically-seeded claims-DB PVC — the restore-verification anchor the OADP backup/delete/restore arc leans on. A second namespace {user}-site-b holds a standalone claims Postgres (the simulated remote site) to expose across an RHSI Connector in the optional cross-site section. The OADP operator + DataProtectionApplication (in-cluster NooBaa S3 BSL) and the Skupper v2 operator are installed by the platform-portfolio resilience stack, NOT this chart; {user}-resilience / {user}-site-b are created + quota'd by the workshop layer (per-user-resilience.yaml), NOT this chart (Rule 13). ws solve materializes the END state: a DR-exercise-complete marker alongside the surviving seeded rows."
type: application
version: 0.1.0
