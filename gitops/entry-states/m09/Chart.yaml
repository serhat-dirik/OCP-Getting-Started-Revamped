apiVersion: v2
name: m09-entry
description: "M09 GitOps Fundamentals — per-user {user}/claims-config fork (kustomize base + dev/stage/prod overlays, personalized to {user}) so the attendee can create their FIRST Argo CD Application on the student-gitops instance and deploy claims into {user}-dev. Entry leaves {user}-dev/stage EMPTY (the Application is the lab). Assumes the workshop-layer student-gitops instance + proj-{user} AppProject + {user}-gitops namespace."
type: application
version: 0.1.1
