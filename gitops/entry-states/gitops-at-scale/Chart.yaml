apiVersion: v2
name: gitops-at-scale-entry
description: "GitOps at Scale & Progressive Delivery — materializes the gitops-fundamentals END STATE per-user (claims GitOps-managed in {user}-dev + {user}-stage via two student-gitops Applications) so gitops-at-scale runs WITHOUT gitops-fundamentals (compose, don't chain), plus the {user}/claims-config fork extended with the rollouts/ overlay (Rollout + AnalysisTemplate + migration hook) and applicationset.yaml source, and the per-user analysis prereqs (claims-analysis SA + gitops-at-scale-canary-control knob) in {user}-prod. Assumes the workshop-layer student-gitops instance (with its ApplicationSet controller) + proj-{user} AppProject + {user}-gitops namespace + the cluster-scoped RolloutManager (platform-portfolio progressive-delivery stack)."
type: application
version: 0.1.3
