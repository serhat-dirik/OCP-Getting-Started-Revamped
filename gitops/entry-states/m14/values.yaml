# Injected by ws start (helm parameters user, clusterDomain, solve); defaults here serve local
# `helm template` dev.
user: user1
clusterDomain: apps.example.com
# ws solve sets true → renders the module END state (instructor/demo/testing): the root-demanding
# workload FIXED to run non-root (scaled up, running) PLUS the "payments team" RBAC the attendee
# builds in the lab (payments-ci edit-in-dev/view-in-prod; payments-ops a scoped "deployer" Role).
# Entry (false) = the pre-lab world: ungoverned teammate SAs + the not-yet-run root-demander.
solve: false
