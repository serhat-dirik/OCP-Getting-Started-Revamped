apiVersion: v2
name: m10-entry
description: "M10 GitOps at Scale & Progressive Delivery — materializes the M09 END STATE per-user (claims GitOps-managed in {user}-dev + {user}-stage via two student-gitops Applications) so M10 runs WITHOUT M09 (compose, don't chain), plus the {user}/claims-config fork extended with the rollouts/ overlay (Rollout + AnalysisTemplate + migration hook) and applicationset.yaml source, and the per-user analysis prereqs (claims-analysis SA + m10-canary-control knob) in {user}-prod. Assumes the workshop-layer student-gitops instance (with its ApplicationSet controller) + proj-{user} AppProject + {user}-gitops namespace + the cluster-scoped RolloutManager (platform-portfolio progressive-delivery stack)."
type: application
version: 0.1.2
