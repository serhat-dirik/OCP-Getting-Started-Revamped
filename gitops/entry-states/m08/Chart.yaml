apiVersion: v2
name: m08-entry
description: "M08 Trusted Software Supply Chain [ADS] — in {user}-cicd: a build→scan→sign pipeline (RHACS scan gate on the seeded Log4Shell CVE + Tekton Chains cosign signing), the attendee's Gitea fork of parasol-claims with a known-vulnerable seed-vulnerable branch, a copied RHACS CI token + the Chains public key. Assumes the platform trust stack (RHACS + trust-signing) and the parasol-tasks library."
type: application
version: 0.1.0
