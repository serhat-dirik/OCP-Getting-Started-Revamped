# Injected by ws start (helm parameters user, clusterDomain, solve); defaults here serve local
# `helm template` dev.
user: user1
clusterDomain: apps.example.com
# ws solve sets true → materializes the developer-hub-golden-paths END state: a representative "already scaffolded" service
# ({user}-svcs/parasol-golden) created from the golden-path skeleton, so instructors get a demo-ready
# post-scaffold world and the end-mode verify has a concrete target. See solve-endstate.yaml. (The
# running deploy of a scaffolded service is an interactive image build in the lab; solve materializes
# the repo outcome, not a built image.)
solve: false
# In-cluster Gitea coordinates. The org + cleanup + solve hooks talk to the Gitea API (admin-credential
# discovery + Sudo, the same idiom as the config-multienv/gitops-fundamentals/gitops-at-scale fork jobs).
giteaNamespace: ogsr-gitea
# In-cluster RHDH base URL. The scaffold-org cleanup hook also deregisters the attendee's catalog
# Location(s) (guest token) so a re-prep leaves no orphan Component on the SHARED portal. The service
# address is fixed by the portal stack (backstage-developer-hub in ns rhdh), so this needs no per-cluster
# injection — the same in-cluster-svc rationale as the Gitea address the golden-path template publishes to.
rhdhInClusterUrl: http://backstage-developer-hub.rhdh.svc
# The golden-path scaffolder (publish:gitea) publishes into a Gitea ORGANIZATION, not a personal
# namespace (verified live: the action's checkGiteaOrg rejects a user namespace). Each attendee gets a
# dedicated org {user}-svcs (created + owned by {user}) as their scaffold target — isolated from their
# personal namespace (which holds seeded forks like {user}/claims-config), so cleanup can safely empty
# it. Suffix is a value so the mechanism stays workshop-agnostic.
scaffoldOrgSuffix: svcs
# The name of the demo service `ws solve` scaffolds (end state). The lab lets the attendee choose their
# own service name; solve uses this fixed one.
solveServiceName: parasol-golden
# Provenance marker stamped into each scaffolded repo's description (by the template's publish step and
# by solve) — documents origin; the cleanup empties the whole dedicated org, so this is not the delete
# selector.
scaffoldMarker: "scaffolded from parasol-service-template"

# BEGIN gen-entry-namespaces manageNamespaces — GENERATED by tools/gen-entry-namespaces.sh, DO NOT EDIT
# This chart's own Namespace/ResourceQuota/LimitRange/RoleBinding render
# (templates/ns-user-namespaces.yaml) is a no-op while this is false. gitops/workshop-config still
# creates all 13 per-user namespaces eagerly today (verified live 2026-08-02: user4-dev carries
# tracking-id workshop-config:/Namespace:openshift-gitops/user4-dev, and the workshop-config
# Application runs prune=true/selfHeal=true) — if this chart ALSO rendered the same Namespace while
# workshop-config still owns it, two Applications would fight over the same object (this chart's
# sync-wave/sync-options annotations stripped by workshop-config's selfHeal, re-added by this
# chart, forever). Flipping this to true is a DELIBERATE migration step that MUST land in the SAME
# change as removing workshop-config's per-user namespace rendering, never before.
manageNamespaces: false
# END gen-entry-namespaces manageNamespaces
