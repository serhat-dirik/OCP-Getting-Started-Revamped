apiVersion: v2
name: m27-entry
description: "M27 DevSecOps [OCP] — in {user}-cicd: the 11-stage parasol-claims-devsecops capstone Pipeline (SAST SonarQube · SCA Trivy · unit-test · build · image-scan RHACS · sign Tekton Chains · config-check roxctl · deploy+Route · DAST ZAP), an ephemeral claims-db, and per-user copies of the SonarQube CI token (sonar-auth) + RHACS CI token (rox-api-token). Assumes the platform appsec stack (SonarQube), the trust stack (RHACS + Chains), and the parasol-tasks library (incl. the four M27 tasks)."
type: application
version: 0.1.0
