# Proves the entry-state engine ran for this user+module; ws verify + ws prep check it.
apiVersion: v1
kind: ConfigMap
metadata:
  name: ws-entry-m17
  namespace: {{ .Values.user }}-dev
  labels:
    workshop.redhat.com/module: m17
    workshop.redhat.com/user: {{ .Values.user | quote }}
data:
  module: m17
  materializedFor: {{ .Values.user | quote }}
  # The single namespace this module spans — M17's registry/catalog world lives entirely in {user}-dev.
  devNamespace: {{ .Values.user }}-dev
