apiVersion: v2
name: devspaces-inner-loop-entry
description: "Dev Spaces & the Inner Loop — claims app + PostgreSQL running in {user}-dev, a per-user Gitea fork of parasol-claims, ready for a workspace"
type: application
version: 0.1.0
