# The per-user Maven local repository, as a claim that OUTLIVES a single PipelineRun.
#
# WHY IT EXISTS. maven-jdk21 used to run `mvn` with the local repository inside the container
# filesystem, so it died with the TaskRun pod and every run re-downloaded the whole dependency
# tree. Measured on the workshop cluster 2026-08-06: a green run of parasol-claims-build-test-deploy
# took 20m49s, and two consecutive runs measured `unit-test` at 11m20s then 11m33s — near-identical
# numbers, i.e. nothing was ever warming. The Pipeline's shared-workspace does NOT solve this: it is
# bound per run (volumeClaimTemplate) and is deleted with the run.
#
# WHY A PLAIN PVC AND NOT A volumeClaimTemplate. A template is materialized per PipelineRun and
# pruned with it — it shares a cache between the Tasks of ONE run, which is not the problem. Only a
# named, already-existing claim survives from one run to the next, which is what the measurement
# says is missing. Bound by name (claimName: maven-cache) from every PipelineRun in this module.
#
# LIFECYCLE. Entry state, so `ws reset` (which runs `oc delete all,pvc --all` in {user}-cicd) drops
# it and the entry app re-creates it empty — a reset costs one cold build, by design: reset means
# "back to the entry state", and a warm cache is not part of the entry state's contract.
#
# COST. One claim against the {user}-cicd quota's `persistentvolumeclaims: "10"`, which also carries
# up to six per-run workspace PVCs during app-security-testing's fix loop (see
# gitops/workshop-config/templates/per-user-limits.yaml). That quota counts CLAIMS, not bytes, so
# the size below is a cluster-capacity question, not a quota question.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maven-cache
  namespace: {{ .Values.user }}-cicd
  labels:
    app: maven-cache
    workshop.redhat.com/module: pipelines-fundamentals
    workshop.redhat.com/user: {{ .Values.user | quote }}
    workshop.redhat.com/owner: ogsr
spec:
  # RWO is correct and sufficient: a user's runs are serial, and one TaskRun pod mounts it at a
  # time. It does pin the run to the node the claim attached to on a topology-aware CSI (the
  # per-run shared-workspace PVC is created fresh, so the scheduler co-locates it).
  accessModes:
    - ReadWriteOnce
  # No storageClassName: take the cluster's default class, like every other entry-state claim
  # (portfolio rule "auto-detect where possible").
  resources:
    requests:
      storage: {{ .Values.mavenCacheSize }}
