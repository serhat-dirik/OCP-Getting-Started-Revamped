# Injected by ws start (helm parameters); defaults here serve local `helm template` dev.
user: user1
clusterDomain: apps.example.com
# ws solve sets true → additionally renders the module END state (instructor/demo mode):
# the claims DB backed by a PersistentVolumeClaim (persistent) instead of emptyDir, PLUS a
# standalone 2-replica PostgreSQL StatefulSet + headless Service (the stateful-workload exercise).
solve: false
