apiVersion: v2
name: trusted-supply-chain-entry
description: "Trusted Software Supply Chain [ADS] — in {user}-cicd: a build→scan→sign pipeline (RHACS scan gate on the seeded Log4Shell CVE + Tekton Chains cosign signing), the attendee's Gitea fork of parasol-claims with a known-vulnerable seed-vulnerable branch, a copied RHACS CI token + the Chains public key. Assumes the platform trust stack (RHACS + trust-signing) and the parasol-tasks library."
type: application
# 0.1.3: the inline sbom-report step's runner is digest-pinned. It floated on `ubi9/openjdk-21:latest`
# while its own comment claimed to be the "same runner as the parasol-tasks maven-jdk21 Task" — which
# is digest-pinned — so the claim held only for as long as the tag happened not to move. The step now
# carries maven-jdk21's reference BYTE-IDENTICALLY (index digest 24dad82b…, image 1.24-3.1785873424),
# making the parity greppable. A supply-chain module whose own SBOM step floats is the contradiction a
# reviewer notices first. Rendering change to the Pipeline, so the bump is load-bearing against Argo's
# manifest cache. Change this and pipelines/tasks/maven-jdk21.yaml together or neither.
version: 0.1.3
