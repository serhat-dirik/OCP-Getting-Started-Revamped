# Proves the entry-state engine ran for this user+module; ws verify + ws prep check it.
# Lives in {user}-dev (M11's home namespace: where the golden-path scaffolds a service).
apiVersion: v1
kind: ConfigMap
metadata:
  name: ws-entry-m11
  namespace: {{ .Values.user }}-dev
  labels:
    workshop.redhat.com/module: m11
    workshop.redhat.com/user: {{ .Values.user | quote }}
    workshop.redhat.com/owner: ogsr
data:
  module: m11
  materializedFor: {{ .Values.user | quote }}
