apiVersion: v2
name: m03-entry
description: "M03 Dev Spaces & the Inner Loop — claims app + PostgreSQL running in {user}-dev, a per-user Gitea fork of parasol-claims, ready for a workspace"
type: application
version: 0.1.0
