# ws reset purges user-created leftovers (workloads/services/routes/pvcs) from these
# namespaces before re-materializing — M01's entry state is "an EMPTY dev namespace".
purgeNamespaces:
  - "${USER}-dev"
