# Injected by ws start (helm parameters user, clusterDomain, solve); defaults here serve local
# `helm template` dev.
user: user1
clusterDomain: apps.example.com
# ws solve sets true → additionally runs the capstone on the CLEAN main branch (every gate passes,
# image deployed + Route created): the demo/instructor END state. See solve-endstate.yaml.
solve: false
# In-cluster Gitea coordinates: fork-and-seed.yaml forks parasol/parasol-claims into {user}/parasol-claims
# and publishes the seed-appsec branch carrying the four app-security-testing flaws (hard-coded credential, vulnerable dep,
# image-baked log4shell, insecure Deployment manifest, missing security headers). The lab runs the capstone
# against that branch (red gates → break-fix → green); `ws solve` runs the CLEAN main (every gate passes).
giteaNamespace: ogsr-gitea
upstreamOrg: parasol
# Shared platform namespaces the per-user hook copies the CI tokens FROM (workshop-agnostic contracts):
#   sonarqube/sonar-ci-token  → the SonarQube analysis token (minted by the appsec stack)
#   stackrox/rox-api-token    → the RHACS CI token (minted by the trust stack)
sonarqubeNamespace: sonarqube
stackroxNamespace: stackrox

# Size of the per-user Maven local-repository claim (templates/maven-cache.yaml). One claim per
# attendee, on the cluster's default StorageClass. 2Gi is headroom, not a measurement: the
# parasol-claims dependency tree has not been sized on disk. Lower it on a storage-tight cluster;
# the {user}-cicd quota caps the NUMBER of claims (10), not their bytes. Changing it AFTER a claim
# exists does not resize that claim — a changed value takes effect on the next `ws reset` (which
# deletes the claim) or not at all.
mavenCacheSize: 2Gi

# BEGIN gen-entry-namespaces manageNamespaces — GENERATED by tools/gen-entry-namespaces.sh, DO NOT EDIT
# This chart's own Namespace/ResourceQuota/LimitRange/RoleBinding render
# (templates/ns-user-namespaces.yaml) is a no-op while this is false. gitops/workshop-config still
# creates all 13 per-user namespaces eagerly today (verified live 2026-08-02: user4-dev carries
# tracking-id workshop-config:/Namespace:openshift-gitops/user4-dev, and the workshop-config
# Application runs prune=true/selfHeal=true) — if this chart ALSO rendered the same Namespace while
# workshop-config still owns it, two Applications would fight over the same object (this chart's
# sync-wave/sync-options annotations stripped by workshop-config's selfHeal, re-added by this
# chart, forever). Flipping this to true is a DELIBERATE migration step that MUST land in the SAME
# change as removing workshop-config's per-user namespace rendering, never before.
manageNamespaces: false
# END gen-entry-namespaces manageNamespaces
