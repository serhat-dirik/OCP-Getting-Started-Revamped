{{- if .Values.solve }}
# END state for instructor demos / `ws solve jobs-batch-kueue`: what the attendee's world looks like after the
# CronJob + statement-Job exercises — a SUSPENDED nightly-statement CronJob and one already-
# completed monthly-statement Job. Both read the seeded claims-data dataset. This is a plain
# (non-Kueue) statement run, matching lab exercise 1 (Kueue admission comes later in the lab and
# leaves no standing end state — its Jobs are ephemeral).
---
# why: one completed monthly-statement Job (parallelism 3, completions 6) — the finished artifact
# of the first exercise. It runs to completion when applied (pods ~10s each). Named -solve so a
# solve AFTER an attendee ran exercise 1 never collides with their identically-named Job — Job
# pod templates are immutable, and the Argo patch would wedge the app OutOfSync (G4-F4).
apiVersion: batch/v1
kind: Job
metadata:
  name: monthly-statement-solve
  namespace: {{ include "jobs-batch-kueue.namespace" . }}
  labels:
    app: monthly-statement
    workshop.redhat.com/module: jobs-batch-kueue
    workshop.redhat.com/user: {{ .Values.user | quote }}
    workshop.redhat.com/owner: ogsr
spec:
  parallelism: 3
  completions: 6
  backoffLimit: 4
  template:
    metadata:
      labels:
        app: monthly-statement
        workshop.redhat.com/module: jobs-batch-kueue
    spec:
      restartPolicy: Never
      containers:
        - name: statement
          image: registry.access.redhat.com/ubi9/ubi-minimal:latest
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 300m, memory: 256Mi}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              DATA=/data/claims.csv
              for i in $(seq 1 30); do [ -s "$DATA" ] && break; echo "waiting for dataset…"; sleep 2; done
              [ -s "$DATA" ] || { echo "no dataset at $DATA"; exit 1; }
              total=$(tail -n +2 "$DATA" | wc -l | tr -d ' ')
              sum=$(tail -n +2 "$DATA" | awk -F, '{s+=$5} END{printf "%.0f", s}')
              echo "monthly statement worker ${HOSTNAME}: processed ${total} claims, total exposure USD ${sum}"
              sleep 8
              echo "statement segment complete"
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: claims-data
---
# why: one small Kueue-managed Job that ran THROUGH the LocalQueue and completed — it leaves a
# Workload object with Admitted=True in its history, the durable evidence that admission control
# was exercised (the lab's own Kueue Jobs are ephemeral and cleaned up). Single pod at 200m/256Mi
# fits the ClusterQueue, so it admits immediately and finishes fast.
apiVersion: batch/v1
kind: Job
metadata:
  name: queued-sample
  namespace: {{ include "jobs-batch-kueue.namespace" . }}
  labels:
    app: queued-sample
    workshop.redhat.com/module: jobs-batch-kueue
    workshop.redhat.com/user: {{ .Values.user | quote }}
    kueue.x-k8s.io/queue-name: user-queue
    kueue.x-k8s.io/priority-class: batch-low
    workshop.redhat.com/owner: ogsr
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 2
  template:
    metadata:
      labels:
        app: queued-sample
        workshop.redhat.com/module: jobs-batch-kueue
    spec:
      restartPolicy: Never
      containers:
        - name: work
          image: registry.access.redhat.com/ubi9/ubi-minimal:latest
          resources:
            requests: {cpu: 200m, memory: 256Mi}
            limits: {cpu: 200m, memory: 256Mi}
          command: ["/bin/sh", "-c", "echo 'admitted through user-queue → cq-{{ .Values.user }}'; sleep 3"]
---
# why: the nightly-statement CronJob, left SUSPENDED (suspend: true) — the state after the
# attendee toggles suspend at the end of the CronJob exercise. concurrencyPolicy Forbid +
# startingDeadlineSeconds mirror the exercise. It will not fire while suspended.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: nightly-statement
  namespace: {{ include "jobs-batch-kueue.namespace" . }}
  labels:
    app: nightly-statement
    workshop.redhat.com/module: jobs-batch-kueue
    workshop.redhat.com/user: {{ .Values.user | quote }}
    workshop.redhat.com/owner: ogsr
spec:
  schedule: "0 2 * * *"          # 02:00 daily — the "nightly batch" window
  suspend: true                  # left suspended (the exercise's final toggle)
  concurrencyPolicy: Forbid      # never overlap a slow run with the next tick
  startingDeadlineSeconds: 120   # if the controller misses the tick by >2m, skip (don't pile up)
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    metadata:
      labels:
        app: nightly-statement          # label the Jobs this CronJob creates so `oc get jobs -l app=nightly-statement` selects them
        workshop.redhat.com/module: jobs-batch-kueue
    spec:
      backoffLimit: 4
      template:
        metadata:
          labels:
            app: nightly-statement
            workshop.redhat.com/module: jobs-batch-kueue
        spec:
          restartPolicy: Never
          containers:
            - name: statement
              image: registry.access.redhat.com/ubi9/ubi-minimal:latest
              resources:
                requests: {cpu: 100m, memory: 128Mi}
                limits: {cpu: 300m, memory: 256Mi}
              command: ["/bin/sh", "-c"]
              args:
                - |
                  set -eu
                  DATA=/data/claims.csv
                  [ -s "$DATA" ] || { echo "no dataset at $DATA"; exit 1; }
                  total=$(tail -n +2 "$DATA" | wc -l | tr -d ' ')
                  sum=$(tail -n +2 "$DATA" | awk -F, '{s+=$5} END{printf "%.0f", s}')
                  echo "nightly statement run: processed ${total} claims, total exposure USD ${sum}"
              volumeMounts:
                - name: data
                  mountPath: /data
          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: claims-data
{{- end }}
