# Proves the entry-state engine ran for this user+module; ws verify checks it (DATA=2).
apiVersion: v1
kind: ConfigMap
metadata:
  name: ws-entry-build-deliver
  namespace: {{ .Values.user }}-dev
  labels:
    workshop.redhat.com/module: build-deliver
    workshop.redhat.com/user: {{ .Values.user | quote }}
    workshop.redhat.com/owner: ogsr
data:
  module: build-deliver
  materializedFor: {{ .Values.user | quote }}
