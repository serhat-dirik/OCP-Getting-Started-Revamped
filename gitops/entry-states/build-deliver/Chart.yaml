apiVersion: v2
name: build-deliver-entry
description: "Ways to Build & Deliver Apps — empty {user}-dev + the attendee's Gitea forks of parasol-claims and parasol-notifications, ready to build from source"
type: application
# 0.1.1: imagePullPolicy: Always on every workshop-BUILT image in this chart. Their tags
# (1.0/1.1) are MUTABLE and rebuilt in place, so the kubelet's IfNotPresent default kept serving a
# node-cached layer and a rebuilt image never reached a running pod (measured on a live cluster, 2026-07-29:
# the IS tag moved to a new digest, `oc rollout restart` brought the OLD digest back up). Upstream
# images pinned to a digest or an immutable tag are deliberately left on IfNotPresent. MUST bump:
# Argo's manifest cache is keyed on the chart version.
# 0.1.2: the solve END state now materializes what the lab actually finishes with — parasol-notifications
# (the polyglot S2I exercise) alongside claims + claims-db, plus the topology labels/annotation
# (app.kubernetes.io/part-of=parasol on all three, app.openshift.io/connects-to on claims) that the
# lab's last step applies and the module's topology screenshot promises.
version: 0.1.3
