# Kiali operator. Channel 'stable' == kiali-operator.v2.27.1 (verified live 2026-07-13, matches
# versions.yaml). The 'candidate' channel holds v2.1.2 — never use it. Automatic approval for z-stream.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: kiali-ossm
  namespace: openshift-kiali-operator
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  channel: stable
  installPlanApproval: Automatic
  name: kiali-ossm
  source: redhat-operators
  sourceNamespace: openshift-marketplace
