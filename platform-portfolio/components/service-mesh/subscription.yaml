# OpenShift Service Mesh 3 (Sail) operator. Channel PINNED to stable-3.3 (== servicemeshoperator3.v3.3.5,
# Istio 1.28.x): the bare 'stable' channel drifted to v3.4.0 (2026-07-13) and 'candidates' holds the
# stale v3.0.0-tp.2 — never use either. ADR-0003 + the ossm-gateway-demo source are validated on the 3.3
# line. installPlanApproval Automatic so z-stream patches inside 3.3 land without a manual gate.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: servicemeshoperator3
  namespace: openshift-service-mesh-operator
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  channel: stable-3.3
  installPlanApproval: Automatic
  name: servicemeshoperator3
  source: redhat-operators
  sourceNamespace: openshift-marketplace
