# Install namespace for OpenShift Lightspeed — the operator, OLSConfig, and its Secret live here.
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-lightspeed
  labels:
    portfolio.redhat.com/component: openshift-lightspeed
