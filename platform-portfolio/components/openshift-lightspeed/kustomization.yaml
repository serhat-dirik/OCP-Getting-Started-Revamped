# OpenShift Lightspeed — own namespace + all-namespaces OperatorGroup + Subscription + OLSConfig.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - olsconfig.yaml
