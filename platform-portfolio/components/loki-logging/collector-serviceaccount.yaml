# ServiceAccount the Vector collector runs as; the ClusterLogForwarder references it and authenticates to LokiStack with its token.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: log-collector
  namespace: openshift-logging
