# Red Hat build of OpenTelemetry operator + a shared OTLP collector (into the observability-workshop namespace).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - otel-collector.yaml
