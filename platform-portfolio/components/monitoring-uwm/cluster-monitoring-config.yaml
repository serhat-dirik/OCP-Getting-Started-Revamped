# Turns on user-workload monitoring — app metrics are curriculum, not an extra.
# Synced with ServerSideApply: this ConfigMap commonly pre-exists on managed clusters.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-monitoring-config
  namespace: openshift-monitoring
data:
  config.yaml: |
    enableUserWorkload: true
