# Own-namespace OperatorGroup: the Gitea operator only needs to watch its namespace.
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: gitea
  namespace: gitea
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  targetNamespaces:
    - gitea
