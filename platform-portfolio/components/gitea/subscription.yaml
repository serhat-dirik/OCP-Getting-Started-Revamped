# Gitea operator subscription from the GPTE catalog above.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: gitea-operator
  namespace: gitea
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  channel: stable
  installPlanApproval: Automatic
  name: gitea-operator
  source: gitea-catalog
  sourceNamespace: openshift-marketplace
