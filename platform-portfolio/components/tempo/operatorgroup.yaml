# All-namespaces OperatorGroup (no targetNamespaces) — tempo-product supports AllNamespaces only (verified live 2026-07-09).
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-tempo-operator
  namespace: openshift-tempo-operator
spec:
  upgradeStrategy: Default
