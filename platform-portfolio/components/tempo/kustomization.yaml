# Tempo Operator + shared observability namespace + a single-pod TempoMonolithic trace store.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - observability-namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - tempomonolithic.yaml
